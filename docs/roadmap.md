# Roadmap

Согласованный план работ (не описание текущего поведения — единственный
файл в docs/, который смотрит вперёд; выполненное отсюда удаляется, а
поведение документируется в соответствующем feature-файле).

## А. Хвосты текущей работы

- [x] А1. Закоммитить весь накопленный дифф (~123 файла) серией
      тематических коммитов, запушить.
- [x] А2. Картинки: 21 172 файла локально, все в WebP (1334 МБ → 803 МБ),
      битых ссылок в БД нет.
- [x] А3. Dockerfile: `playwright install --with-deps chromium` в
      runner-образ — иначе кнопка blscene падает на проде.
- [x] А4. (руками, @BotFather) `/setdomain` → myblhub.com для
      @myblhub_bot — сделано владельцем 2026-08-18.

## Б. Блокеры прода

- [x] Б1. Реверс-прокси с HTTPS (Caddy) в docker-compose, домен
      myblhub.com → сервер.
- [x] Б2. Бэкапы: pg_dump по крону + копия public/uploads.
- [x] Б3. Rate-limiting на логин/регистрацию (in-memory, без внешних
      зависимостей).
- [x] Б4. Admin-сессии: серверные сессии вместо куки=секрет (по образцу
      пользовательских).
- [x] Б5. Регистрация по инвайтам (код при регистрации, генерация в
      админке).
- [x] Б6. Прод-гигиена: /api/health + healthcheck, CI, robots.txt —
      сделано; Sentry отложен (нужен аккаунт/DSN — решить отдельно).

## В. Подписка

- [x] В1. Срок подписки: `premiumUntil DateTime?` вместо вечного
      boolean; выдача из админки — на месяц; истечение проверяется в
      гейтах; напоминание в Telegram за 3 дня до конца.
- [x] В2. Оплата через Telegram (Stars/Payments): кнопка в заглушке →
      инвойс в боте → вебхук продлевает premiumUntil на месяц.
- [x] В3. Продающая заглушка подписки: что внутри, цена, кнопка оплаты.

## Г. Функционал

- [x] Г1. Пресейл-напоминания в Telegram («через час открываются
      продажи») — для событий из избранного/«иду».
- [x] Г2. Уведомления о друзьях («Аня идёт на …») + управление на сайте:
      подписаться/отписаться на уведомления конкретного друга.
- [x] Г4. Вкладка «Что посетить» в поездке: локации съёмок дорам
      (приоритет — из избранного/просмотренного) рядом с датами поездки,
      с картой.
- [x] Г6. Заметки к событиям — личные или видимые друзьям.
- [x] Г7 (часть). Оффлайн-кэш PWA (sw.js: network-first страницы,
      cache-first статика). Web-push отложен — Telegram уже покрывает
      уведомления; вернуться, если понадобится браузерный канал.
- [x] Г8. Настройки приватности профиля (скрыть активность/актёров;
      сейчас профиль виден всем залогиненным).
- [x] Г10. Админ-дашборд: юзеры/подписки/последние импорты одним
      экраном.

## Д. Геймификация

- [x] Д1. Вкладка «Статистика» в кабинете: счётчики (концерты, площадки,
      актёры вживую, локации, дорамы, поездки, дни в Таиланде), топ
      актёров по посещениям, карта посещённого, разбивка по годам.
- [x] Д2. Ачивки (~20, считаются из существующих данных): бейджи в
      кабинете и публичном профиле, прогресс до следующей,
      поздравление от бота в Telegram при получении.

Отложено из геймификации: «Итоги года» (к декабрю), уровни/лидерборды
(посмотреть, как зайдут ачивки).

## Сделано сверх плана (по ходу сессии)

- «Иду» на конкретную дату события (per-occurrence attendance).
- Списки мест (PlaceList) с видимостью, привязкой к поездкам и
  собственными местами по ссылке Google Maps.
- Оптимистичные кнопки избранного/«иду», перенос состава в инфо-карту,
  «Я ходил(а)» для прошедших, дедуп «друзья идут».

## Отложено (обсуждали, решили пока не делать)

- Лента «что нового» (Г3), экспорт плана поездки в .ics (Г5),
  Telegram mini-app (Г9).

## Идеи на будущее (не в работе)

- Создание событий из фото афиши через Telegram (обсуждено 2026-08-22,
  отложено — требует платного API, а проект пока бесплатный).
  Менеджер кидает боту фото афиши → vision-модель (Claude API)
  вытаскивает название/даты/время/площадку/артистов → бот присылает
  превью с матчингом артистов по базе и inline-кнопками
  «Создать / Править / Отмена» → событие создаётся на сайте.
  Половина уже есть: вебхук бота (src/app/api/telegram/webhook),
  паттерн превью→апрув и матчинг по никнеймам в TTM-импорте
  (admin/events/importActions.ts), createEvent. Нужно новое:
  ANTHROPIC_API_KEY (≈цент за афишу, оплата через посредника типа
  OpenRouter), обработка photo и callback_query в вебхуке, модель
  черновика EventDraft. В промпт заложить буддийские годы на тайских
  афишах (2569 = 2026).

## Ошибки server actions → в возвращаемое значение (TODO)

- [x] Сделано 2026-08-19: промокод и форма обращений возвращают
      `{ ok, error }`, остальные вызывающие (ConfirmForm,
      FriendActionButton, комбобоксы, CreatePairingModal, кнопка
      blscene) показывают понятный текст вместо минифицированной
      ошибки React, возврат платежа отдаёт причину значением.

## Списки: подгружать данные с сервера порциями (TODO)

- [x] /locations и вкладка «по сериалам» переведены на загрузку по
      буквам (2026-08-19): 1.27 МБ → 233 КБ и 1.75 МБ → 334 КБ.
- [x] /locations переведены на передачу данных вместо разметки
      (2026-08-19): список остаётся целиком на странице, переход по
      букве — обычный скролл, вес 232 КБ и 287 КБ вместо 1.27 и 1.75 МБ.
- [x] /artists переведён на данные вместо разметки (2026-08-19):
      278 КБ → 126 КБ, порционная отрисовка при скролле.

## Интерактивный тур по сайту после регистрации (TODO)

- [x] Сделано 2026-08-19: семишаговый тур по интерфейсу, см.
      features/onboarding.md.

## Уведомления пользователю на сайте

- [x] Сделано 2026-08-19 (см. features/notifications.md): колокольчик в
      шапке со счётчиком (десктоп и мобильный), лента /notifications с
      пагинацией, уведомления о приглашениях в поездки, заявках в
      друзья, ответах на комментарии и лайках, дублирование важного в
      Telegram и кнопка «Подключить Telegram» в настройках аккаунта.
- [x] Доделано 2026-08-19: «друг идёт», выданная подписка и ачивки
      идут через notifyUser; что дублировать в Telegram — настройка
      в профиле.

## Источники у локаций (TODO)

- [x] Сделано 2026-08-19: у Location появился sourceUrl, все 566
      каталожных локаций получили ссылку на страницу сериала в blscene,
      импортёр проставляет её новым, на странице есть блок «Источники».
      У пользовательских мест источника нет.

## Отметка «видел(а) исполнителя вживую» вручную (TODO)

- [x] Сделано 2026-08-19: кнопка «видела вживую» на странице
      исполнителя, статистика считает объединение с автоматическими
      отметками.

## Мои билеты — отдельным разделом (TODO)

- [x] Сделано 2026-08-19: вкладка «Билеты» в кабинете — все файлы с
      событием, датой и площадкой, ближайшие сверху.

## Места: ссылки и категории (TODO)

- [x] Сделано 2026-08-19: у мест есть ссылки (инстаграм, сайт,
      канал), правятся в админке.
- [x] Сделано 2026-08-19: категории мест с фильтром в списке и
      значком на странице; правятся в админке.

## Поездки: брони отелей (TODO)

- [x] Сделано 2026-08-19: брони жилья в поездке — файл, адрес, даты
      заезда и выезда, видны участникам поездки.

## Привязка Telegram в настройках

- [x] Сделано 2026-08-19: в строку проверки подписи идут только поля,
      которые подписывает Telegram — свой `?mode=link` больше её не
      ломает.

## Кнопки на карточке актёра

- [x] Сделано 2026-08-19: сердечко и «+ в список» собраны в одну группу
      (раньше justify-content-between разносил их по ширине).

## Кнопка «в список» на странице актёра

- [x] Сделано 2026-08-19: «+ в список» рядом с сердечком, выбор списка в
      модалке, уже добавленные помечены.

## Что закрыть подпиской

- [x] Личная статистика, ачивки и создание списков актёров — по
      подписке (2026-08-19). Уже созданные списки у владельцев
      остались доступны, навигационные счётчики тоже открыты всем.
      См. features/gamification.md.

## Выпадающий календарь закрывается при выборе месяца/года (TODO)

- [x] Сделано 2026-08-19: проверяются оба контейнера (поле и портал),
      плюс закрытие по клику мимо пикера.

## Пустые блоки на страницах (TODO)

- [x] Сделано 2026-08-19: убраны пустой блок «События» у актёра,
      пустой «Актёрский состав» у сериала и «Сериалы» у локации.
- [x] Пройдено по страницам сущностей (2026-08-19): пустых разделов не
      осталось. На странице агентства сообщения вида «Никого не
      нашлось» оставлены сознательно — это результат поиска по вкладке,
      а не пустой раздел.

## Жалобы: показывать, на кого пожаловались (TODO)

- [x] Сделано 2026-08-19: в очереди видно имя (или почту) того, на кого
      пожаловались, плюс бейдж «жалоб: N» при повторных обращениях на
      одного человека.

## Админка: пагинация и сброс счётчиков (TODO)

- [x] Сделано 2026-08-19: пагинация в /admin/imports и /admin/errors,
      кнопка «Пометить разобранными» и поле reviewedAt — бейджи в
      сайдбаре считают только неразобранное.

## Админка: незакрытые правки (TODO)

- [x] Массовые действия в списках каталога (исполнители, агентства,
      сериалы) — см. features/audit-log.md.
- [x] История изменений записи: AuditLog, блок на карточке и лента
      /admin/history — см. features/audit-log.md.
- [x] Музыку исполнителя — вкладкой формы с раздельным сохранением
      (панель вне <form>, таб-бар общий).

## Админские уведомления

- [x] Telegram админам (обращения, жалобы, упавшие импорты, ошибки,
      оплаты) + бейджи-счётчики в сайдбаре — см.
      features/admin-notifications.md. Почтовый канал ждёт SMTP
      (см. ниже).

## Сборка образа — вынесена с прод-сервера

- [x] Образ собирается в GitHub Actions и приезжает из ghcr.io; на
      сервере только `pull` и перезапуск (2026-08-19). Раньше сборка шла
      на боевой машине и на 8–10 минут душила сайт.

## Подтверждение почты при регистрации (TODO)

- [ ] Сейчас почта при регистрации не проверяется: можно указать чужой
      или несуществующий адрес, и это выяснится только когда письмо не
      дойдёт. Нужно письмо со ссылкой подтверждения и пометка у
      аккаунта (emailVerifiedAt), а до подтверждения — ограничение на
      то, что требует почты (сброс пароля, уведомления на почту).
      Ждёт почтового сервиса — см. раздел «Почта».

## Оплата Stars (в процессе, 2026-08-18)

- [ ] Оплата падает с `PROVIDER_ACCOUNT_INVALID`. Инвойс создаётся
      нормально (проверено прямым запросом к API), Stars у бота
      подключены (getMyStarBalance отвечает) — значит дело в аккаунте.
      По Live Checklist Telegram: нужна двухэтапная аутентификация на
      аккаунте-владельце бота (за владельцем) и команды /terms,
      /support (сделано). Если не поможет — писать @BotSupport;
      возможны региональные ограничения (BotFather предлагает только
      белорусских и узбекских провайдеров).
- [ ] Пока Stars не работают: продавать подписку промокодами
      (генерация в админке уже есть). На перспективу — платёжный сервис
      на сайте (ЮKassa/Robokassa работают с самозанятыми).

## Почта (отложено 2026-08-18, решение за владельцем)

- [ ] Выбрать и подключить почтовый сервис. Ограничение: аудитория в
      РФ, Cloudflare-решения не подходят. Исследовано:
      бесплатных ящиков на домене с SMTP не осталось — Яндекс 360 и
      VK WorkSpace платные (~250–310 ₽/юзер/мес), у Zoho бесплатный
      тариф только webmail (без SMTP/IMAP). Бесплатное комбо: Zoho
      Free (ящик admin@, читать через веб) + Unisender Go (SMTP,
      1500 писем/мес бесплатно, российский). Проще всего — платный
      Яндекс 360: один сервис закрывает и ящик, и SMTP
      (smtp.yandex.ru:465, пароль приложения). Шаги — в docs/deploy.md.
      Код готов весь: SMTP_* в .env + admin_notify_email в настройках.
- [ ] Пока почты нет: сброс пароля недоступен (вход через
      Google/Telegram работает), ответы на обращения — с личного ящика
      через mailto, админ-уведомления — только Telegram.

## Обращения и жалобы → почта

- [x] Код готов: notifyAdmins дублирует уведомления письмом на адрес из
      настройки `admin_notify_email`. Ждёт от владельца ящик на домене
      (admin@myblhub.com) и SMTP-доступы в .env — до этого канал молча
      пропускается, всё видно в /admin/feedback и /admin/moderation.

## Вики-статьи

- [x] Стартовые статьи вычитаны и опубликованы владельцем
      (2026-08-19). Черновики заливал `scripts/seed-wiki-drafts.ts`.

## SEO и открытие каталога (обсуждено 2026-08-17, решение за владельцем)

- [x] Каталог открыт без логина (2026-08-17, см. auth.md): proxy
      пропускает /artists, /dramas, /novels, /locations, /agencies,
      /day, /event, /search; действия редиректят анонима на /login.
- [x] generateMetadata на деталках сериалов/артистов, app/sitemap.ts
      (весь открытый каталог), JSON-LD Person/TVSeries
      (src/lib/seo.tsx). OG-картинки — из постеров/фото.
- [x] Google Analytics и Яндекс.Метрика подключены владельцем через
      DNS-запись (2026-08-19).
- [x] Яндекс.Метрика подключена кодом (2026-08-19, счётчик из
      YANDEX_METRIKA_ID).
- [x] Sentry подключён (2026-08-19): серверные и клиентские ошибки,
      DSN из env. Свой /admin/errors остаётся как быстрый взгляд.
- [x] Google Search Console и Яндекс.Вебмастер: домен подтверждён,
      sitemap.xml отправлен владельцем (2026-08-22).
- [ ] Картинки: сейчас <img> без оптимизации из /uploads. Cloudflare
      НЕ подходит — аудитория в РФ, а его проксирование там работает
      плохо. Варианты: кэш-заголовки + сжатие на нашем же Caddy
      (дёшево, сделать первым), next/image, либо РФ-дружелюбный CDN
      (Yandex Cloud CDN / Selectel), когда вырастет трафик.
      → влит в Э0/Э2 ниже.

## Э. Большая доработка по аудиту 2026-08

Детали и обоснования — docs/audit-2026-08.md, дизайн-референсы —
docs/design-direction.md. Решения владельца (2026-08-22): порядок — на
усмотрение Claude (Э0 → Э1 → дальше); дизайн — тёмная тема + оранжевый,
админка остаётся фиолетовой; голос сайта — «мы» (команда), обращение к
аудитории нейтральное; себя ни с кем не сравниваем; подписка остаётся
платной, но нужно продавать её пользой; публичную оплату пока прячем;
владелец — самозанятая в Беларуси, юр-базу делаем сами без юристов;
вики-статьи владелец пишет сама (на проде сейчас 1 статья).

### Э0. Быстрые техфиксы (скорость и стабильность)

- [x] Э0.1 React.cache на getCurrentUser (2026-08-22) — один запрос
      сессии на HTTP-запрос вместо 2-3.
- [x] Э0.2 @@index на горячие поля (2026-08-22): 35 индексов в 34
      моделях — обратные стороны join-таблиц (EventPerformer.performerId
      и т.п.), FK, поля сортировки (Performer.name, Drama.title,
      EventOccurrence.startsAt), миграция add_hot_path_indexes.
      Отложено: trigram/GIN под ILIKE-поиск — вернуться, если поиск
      останется медленным после остальных фиксов.
- [x] Э0.3 loading.tsx (2026-08-22): PageSkeleton + .skeleton-* в
      globals.css, по одному loading.tsx на (public) и admin/(protected)
      — навигация сразу показывает шиммер. Тонкая настройка скелетов
      под конкретные страницы — в Э2.5.
- [x] Э0.4а error.tsx (2026-08-22): (public)/error.tsx,
      admin/(protected)/error.tsx (показывает message и digest),
      global-error.tsx + Sentry.captureException. Внимание: в этой
      версии Next проп называется retry, а не reset.
- [ ] Э0.4б server actions → контракт { ok, error } вместо 96 throw с
      русским текстом (публичные — приоритет, админские можно позже:
      их error.tsx теперь показывает текст).
- [x] Э0.5 N+1 в /trips (2026-08-22): три batch-запроса на все поездки
      вместо 2×COUNT на каждую; счётчики считаются в JS. e2e
      shared-trips зелёные.
- [ ] Э0.6 chromium.launch из server actions (короткие Maps-ссылки) →
      резолв HTTP-редиректом без браузера; mdlClient headless:false —
      проверить, как это живёт на сервере.
- [ ] Э0.7 Пагинация/облегчение тяжёлых страниц: /admin/events,
      /locations/map (select вместо полных строк), /admin/duplicates,
      календарь месяца, /account.
- [ ] Э0.8 Кэш: unstable_cache с TTL на каталожные выборки, дашборд
      админки, sitemap; выкинуть no-op revalidatePath («/performers» —
      11 вызовов на переименованный маршрут).
- [ ] Э0.9 uniqueCatalogSlug: убрать цикл до 50 findFirst на каждый
      create (медленные импорты).
- [ ] Э0.10 Мелкие баги админки: «Всего» в /admin/users, мёртвый
      Promise.resolve в pairings, пустой performers в
      CreatePairingModal, count() на неактивной вкладке imports.

### Э1. Юридический минимум (РБ 99-З + оглядка на 152-ФЗ)

- [ ] Э1.1 Закрыть загруженные файлы: /uploads/tickets и брони отелей
      отдавать через route-handler с проверкой сессии (владелец/
      участники поездки), убрать их из публичной статики Caddy.
- [ ] Э1.2 /privacy — политика конфиденциальности: что собираем, зачем,
      третьи сервисы (Метрика, Sentry, Telegram, Google), трансграничка,
      срок хранения, права пользователя, контакт.
- [ ] Э1.3 Cookie-баннер: Метрика (особенно Вебвизор) стартует только
      после согласия; Sentry Replay — решить там же.
- [ ] Э1.4 Правка /terms: убрать «данные не передаются третьим лицам»,
      реквизиты (самозанятая, Республика Беларусь), процедура для
      правообладателей с контактом и сроком.
- [ ] Э1.5 Регистрация: чекбокс согласия с /terms и /privacy (в т.ч.
      для входа через Google/Telegram — текст под кнопками).
- [ ] Э1.6 Спрятать публичную оплату: PremiumUpsell без Stars-кнопки →
      «напишите нам» + промокоды; Stars-кнопка возвращается настройкой,
      когда оплата заработает и решим юр-вопрос. Упоминания оплаты в
      /terms сократить до факта «доступ выдаётся по промокоду/по
      договорённости».
- [ ] Э1.7 Самоудаление аккаунта в настройках (softDeleteUser + экран
      подтверждения).
- [ ] Э1.8 Блок «Источники» у дорам (blscene/MDL), событий (TTM),
      агентств (Wikipedia/fandom).
- [ ] Э1.9 /friends: не показывать чужой email в результатах поиска.
- [ ] Э1.10 Рассылка /admin/broadcast уважает тумблеры уведомлений;
      кнопка «пожаловаться» на комментарии и отзывы.
- [ ] Э1.11 Чистка данных по крону: просроченные UserSession и
      PasswordResetToken.

### Э2. Дизайн: «вау»-фронт (см. docs/design-direction.md)

- [ ] Э2.1 Примитивы: Card/Surface, Badge/Chip, PageHeader, EmptyState
      (с CTA), Poster/Thumb, SubmitButton (useFormStatus) — заменить
      204 инлайн-стиля и 17 копий шапки.
- [ ] Э2.2 Главная залогиненного: bento-мозаика с постерными
      карточками вместо стопки плашек.
- [ ] Э2.3 Лендинг: hero с продуктом (скриншоты/постеры), тёплые
      градиенты, нумерованные секции, честные обещания (без «доступ ко
      всей афише»).
- [ ] Э2.4 Карточки контента: постер как основной визуал; различать
      типы (событие/дорама/артист/локация) оформлением.
- [ ] Э2.5 Анимации: stagger-появления, переходы страниц, hover;
      скелетоны из Э0.3 в той же стилистике.
- [ ] Э2.6 Мобильная: drawer вместо dropdown (Esc/клик мимо/свайп),
      нижний таб-бар для PWA, календарь на узких экранах.
- [ ] Э2.7 Картинки: next/image или ресайз-варианты + кэш; blur-up
      плейсхолдеры; починить sw.js (версионирование кэша uploads);
      themeColor/manifest привести к фактической палитре.
- [ ] Э2.8 Пустые состояния и 404 с иллюстрацией и CTA.
- [ ] Э2.9 docs/design-system.md — зафиксировать примитивы и правила.

### Э3. Тексты и идентичность

- [ ] Э3.1 Единая формулировка «кто мы» (от лица «мы», без сравнения с
      MyDramaList — упоминание убрать) и единый словарь: артист,
      дорама, фанмит, афиша — везде одинаково.
- [ ] Э3.2 Страница «О проекте» с настоящим текстом (сейчас /about =
      лендинг).
- [ ] Э3.3 Подписка: продающий текст про пользу и уникальный контент
      (голос — как в ачивках/вики), а не список запертых разделов.
- [ ] Э3.4 Пустые состояния, тултипы, письма бота — переписать в одном
      голосе.
- [ ] Э3.5 Меты: generateMetadata на /events, /day, /users/[id],
      /artist-lists/[id]; разнообразить шаблонные description.
- [ ] Э3.6 README.md: ThaiHub → MyBLHub, актуальное описание.

### Э4. Админка

- [ ] Э4.1 Единые примитивы: SubmitButton, PageHeader, DataList-строка,
      один ConfirmForm везде (заменить 3 window.confirm), один
      PAGE_SIZE, один комбобокс (убрать копии из Drama/EventForm).
- [ ] Э4.2 Пагинация и поиск во всех списках: events, wiki, feedback,
      finance, broadcast, pairings, moderation (поиск), duplicates.
- [ ] Э4.3 Разбор performers-монолита: агентства — свой раздел со своим
      page.tsx, пейринги — пункт меню; PerformerForm — посекционное
      сохранение.
- [ ] Э4.4 Перегруппировка меню (разгрузить «Сервис»), группы в
      мобильном меню, убрать мёртвый /admin/stats.
- [ ] Э4.5 FormSection + UnsavedGuard во все формы; ошибки валидации
      видимы с любой вкладки.
- [ ] Э4.6 /admin/errors: группировка по digest.

### Э5. Тесты и надёжность

- [ ] Э5.1 Смоук вебхука Telegram: секрет, /start, оплата (моки),
      идемпотентность по charge id.
- [ ] Э5.2 Тесты премиум-гейтов и промокодов (не утечёт ли платный
      контент).
- [ ] Э5.3 e2e в CI (webServer + тестовая БД в GitHub Actions).
