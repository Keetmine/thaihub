# Сообщества (АА25)

Место, где люди находят своих: «Лакорны Беларусь», фан-клуб пейринга,
компания на фестиваль. Заводит обладатель подписки, вступает любой
вошедший.

Идея и разбор — обсуждение с владельцем 2026-09-08. Решения, принятые
тогда же и определяющие всё остальное:

- **чата нет и в планах его нет первым** — обсуждения дают то же самое
  на готовой инфраструктуре, а живой чат означал бы вебсокеты,
  круглосуточную модерацию и хранилище картинок. Вместо своего чата —
  ссылка на чужой (у людей уже есть Telegram-чаты, где они умеют
  общаться и модерировать);
- **витрина — всем, содержимое — участникам**;
- **создание по подписке, участие бесплатно.**

## Что уже сделано (этап 1)

Ядро: модель, создание, страница сообщества, вступление и выход,
заявки, витрина, пункт в меню.

## Файлы

- **`prisma/schema.prisma`** — `Community`, `CommunityMember`,
  `CommunityLink` + enum'ы `CommunityVisibility`, `CommunityJoinMode`,
  `CommunityRole`, `CommunityMemberStatus`.
- **`src/lib/communities.ts`** — `communityAccess`: кто что видит.
  Чистая функция, без базы — правило доступа должно читаться целиком в
  одном месте. Там же `viewerCommunitiesWhere` — то же правило для
  выборок: «сообщества, где зритель `ACTIVE`». Через него ходят и
  `viewerMeetupsWhere`/`viewerEventsWhere` (`catalogEvents.ts`), и блок
  главной.
- **`src/app/(public)/communities/actions.ts`** — создание, правка,
  удаление, вступление, выход, решения по заявкам, ссылки.
- **`src/app/(public)/communities/CreateCommunityButton.tsx`** — окно
  создания со всеми полями сразу (обложка, место, ссылки).
- **`src/app/(public)/communities/page.tsx`** — витрина.
- **`src/app/(public)/communities/[id]/page.tsx`** — страница
  сообщества + клиентские `JoinButton`, `MemberRequests`,
  `CommunityAdmin`.

## Кто что видит

Правило одно и важнее прочих: **витрина — всем, содержимое —
участникам**. Внутри живут ссылки на закрытые чаты, а на следующем
этапе появятся встречи с адресами; наружу это не уходит ни гостю, ни
поисковику.

| | публичное | закрытое |
| --- | --- | --- |
| в списке `/communities` | да | нет |
| в поиске (индексация) | да | нет |
| гость на странице | обложка, название, описание, число участников | название + «закрытое» |
| участники, ссылки | только участникам | только участникам |
| вкладки «Обсуждения» и «Встречи» | видны, но закрыты: заголовки ПУБЛИЧНЫХ тем без ссылок и карточки встреч с одной датой | нет |
| приватные темы (`CommunityPost.isPrivate`) | только участникам, снаружи их нет и в счётчике | нет |

Витрина публичного сообщества открыта намеренно: человек ищет «лакорны
Беларусь», находит страницу и заводит аккаунт, чтобы вступить. Это
единственный правдоподобный источник притока — сообщества умирают не от
плохого функционала, а оттого, что их никто не находит.

Закрытое сообщество страницу всё-таки отдаёт (по прямой ссылке), но
только с названием: «страницы нет» было бы враньём, а содержимое всё
равно закрыто.

## Создание: все поля сразу

Форма создания (`communities/CreateCommunityButton.tsx`, окно) спрашивает
**всё, что есть у сообщества**, — правка владельца 2026-09-09: «при
создании сообщества сразу выводим все поля и со странами и с фотками и
остальное». До этого спрашивались название, описание и две настройки, а
обложку, место и ссылки приходилось добавлять потом в «Управлении» — про
них попросту не узнавали, и сообщество оставалось пустым.

Группировка — та же, что в окне управления
(`[id]/CommunityAdmin.tsx`), и подписи берутся из тех же строк словаря:
два разных набора слов для одних и тех же полей читались бы как два
разных места.

1. название и описание;
2. **обложка** — ПОД описанием (правка владельца 2026-09-09): сообщество
   начинают заводить со слов, а не с картинки. Выше «Создать» она всё
   равно с запасом, так что файл успевает уехать, пока дописывают
   остальное;
3. «кто может найти», «вступление»;
4. **место** — просто «Страна» и «Город», без заголовка и пояснения над
   ними (правка владельца 2026-09-09: поля с примерами в плейсхолдерах
   понятны сами, а абзац про витрину и фильтр объяснял устройство сайта
   тому, кто просто заводит сообщество). Заголовок убран и в управлении —
   строки `topics.placeTitle`/`placeHint` осиротели и удалены из словарей;
5. **ссылки** — до `COMMUNITY_LINKS_AT_CREATE_MAX` (5) рядов «подпись +
   адрес»; ряд добавляется кнопкой (пять пустых пар сразу читались бы как
   требование их заполнить) и убирается КРЕСТИКОМ в том же ряду, что и
   поля (жалоба владельца 2026-09-09: «накликал десять, а удалить
   нельзя», и следом — подписанная кнопка «Убрать ссылку» весила больше
   самих полей). Подпись живёт в подсказке по наведению и в aria-label,
   строка та же, что у готовой ссылки в управлении. Значения
   рядов живут в состоянии: у неуправляемых полей удаление ряда сдвинуло
   бы чужой текст на освободившееся место.

**Обязательным осталось одно название.** Сообщество заводят на
настроении, и пять обязательных полей на входе — верный способ не
завести его вовсе.

Обложка грузится **сразу при выборе файла**, как в управлении и в форме
встречи: кадр уезжает на `/api/upload`, а в форме остаётся только адрес
скрытым полем `coverUrl` (сообщества ещё нет, писать адрес некуда).
Пока файл едет, сабмит закрыт и кнопкой, и проверкой в обработчике —
иначе сообщество молча сохранилось бы с пустой обложкой; форму
отправляет ещё и Enter в текстовом поле, мимо кнопки.

Правила проверки у создания и у управления **общие**, одним набором
функций в `src/lib/communities.ts`: `parseCommunityPlace` (город без
страны не бывает), `parseCommunityLink` (нужны обе половины, адрес
только `http(s)`), `parseCommunityCoverUrl` (только свои `/uploads/…`
без `..`). Копии этих условий разъехались бы молча — и разошлись бы
ровно в ту сторону, где на витрину попадает ненаходимый город или чужой
домен в `<img src>`.

## Вступление

Настройка владельца, спрашивается сразу при создании:

- **свободное** — нажал «Вступить» и стал участником;
- **по одобрению** — заявка (`CommunityMember.status = PENDING`),
  владельцу уходит уведомление `COMMUNITY_JOIN_REQUEST`, он принимает
  или отклоняет.

Решение по заявке уведомляет заявителя в обе стороны
(`COMMUNITY_JOIN_ANSWER`): молчание в ответ на заявку выглядит хуже
внятного отказа. Отдельного вида уведомления для отказа нет — приставка
`+`/`-` в `subject` различает ветки, а фраза собирается на языке
получателя (см. `notificationText.ts`).

В закрытое сообщество заявок с улицы нет вовсе: туда зовёт владелец.

Создатель сразу состоит в своём сообществе строкой `CommunityMember` с
ролью `OWNER` — иначе его не было бы в списке участников. Выйти из
своего сообщества он не может: оно осталось бы без хозяина, для этого
есть «удалить».

## Ограничения

- **Создание — по подписке** (`isPremiumActive`), как у списков мест.
  Платное тут только создание: вступать, читать и участвовать можно без
  подписки.
- **Не больше `COMMUNITY_LIMIT_PER_USER` (3) сообществ на человека.**
  Тридцать сообществ по одному участнику портят витрину сильнее, чем их
  отсутствие.
- Ссылки принимаются только с `http(s)://`: `javascript:` в чужой
  ссылке — это уже атака на того, кто её откроет.
- Витрина отсортирована по числу участников, а не по дате: пустое
  сообщество, заведённое вчера, наверху выглядит плохо.
- Витрина идёт **во всю ширину страницы** (правка владельца
  2026-09-09), в 44rem остался только вступительный абзац, где
  ограничение работает на читаемость. Карточки лежат вертикальным
  списком `.community-list` — по одной на строку (правка владельца
  2026-09-09): в сетке по 18rem карточке не хватало ширины на обложку и
  описание. Внутри карточки слева обложка сообщества (`coverUrl`) —
  КВАДРАТОМ 4rem (правка владельца 2026-09-09): полоса 3:2 съедала в
  строке место у названия, а квадрат встаёт в один ряд с аватарками
  остальных списков. Квадрат с тех пор стал общим: обложка квадратная
  и на самой странице сообщества, и в профиле, и в кадрирующей рамке
  (правка владельца 2026-09-09, см. «Обложка»). Справа
  название, описание и строка «N участников · Страна, Город». Без
  загруженной обложки показывается первая буква названия — общим
  `LetterAvatar`, тем же, что рисует аватарки в остальных списках. Ряд
  фильтра по стране — общий `ScrollableTabs`: стран может быть сколько
  угодно, и ряд прокручивается, а не заворачивается.

## Слаг

`Community` в `FREE_SLUG_MODELS` (`src/lib/prisma.ts`): **чистый адрес,
пока имя свободно**, и короткий код — только при совпадении (правка
владельца 2026-09-09: «а почему в ссылке в конце ttbc?»). Сообщество —
вещь публичная, его адресом делятся и читают глазами, а тёзок у него
единицы; поездки и списки мест кода не лишились — их заводят десятками.
Нумерации `-2`/`-3` при совпадении по-прежнему нет: она выдавала бы,
сколько таких сообществ уже завели.

Проверка «занято ли» и сама вставка — разные запросы, поэтому двое,
заводящие тёзок в одну секунду, могут договориться об одном адресе.
Ошибку уникальности в этом случае человеку не показываем: расширение
ловит `P2002` по `slug` и повторяет вставку с кодом. Адреса УЖЕ
созданных сообществ не трогаются — по ним ходят ссылки.

## Обсуждения

Вкладка «Обсуждения» — то, ради чего в сообщество возвращаются.
Собственного чата у нас нет и не будет первым (решение владельца
2026-09-08): обсуждения дают то же самое на инфраструктуре, которая уже
написана.

Тема — `CommunityPost`: необязательный заголовок, текст, флаг `pinned`.
Заголовка может не быть намеренно: половина разговоров начинается
репликой («кто идёт на фанмит?»), и обязательное поле заставляло бы
придумывать ей название. Там, где нужна вывеска (строка списка,
заголовок страницы, `<title>`), вместо него показывается начало текста.

### Список тем и страница темы

**Вкладка — список, тема — своя страница.** Сначала все темы вместе со
всеми ответами лежали на вкладке одной лентой, и читать её было нечем:
«если там будет 100500 фоток, то как листать» (жалоба владельца
2026-09-08).

- **вкладка** `?tab=discussions` — строки тем: заголовок (или начало
  текста), автор, дата, число комментариев, пометка закреплённой.
  Порядок прежний: закреплённые сверху, дальше свежие. Закреп для того и
  нужен, чтобы правила сообщества и знакомство не тонули под новыми
  темами. Наверху — оранжевая кнопка **«Новая тема»**, открывающая
  форму **модальным окном** (правка владельца 2026-09-09). Раньше форма
  разворачивалась прямо на вкладке и на время написания сдвигала список
  вниз, а звалась тихой кнопкой «Завести тему» — главное действие
  вкладки не должно прятаться в цвет фона;
- **страница темы** `/communities/<slug>/posts/<id>` — сам разговор:
  текст, картинки, лента комментариев с ответами и лайками, форма
  ответа.

Комментарии в списке НЕ выбираются: строке нужно одно число, и оно
считается в базе (`_count` с фильтром), а не пересчитывается из
привезённых реплик. Иначе список из полусотни тем тянул бы за собой все
их треды с картинками — ровно то, от чего страница темы и появилась.

Закреп, правка и удаление темы есть в обоих местах: это быстрые
действия хозяев сообщества и автора, и ради них незачем заходить в
каждую тему. Правка появилась в списке правкой владельца 2026-09-09 —
опечатку замечают как раз в строке, а «изменить» было только на
странице темы. В строке она стоит **иконкой-карандашом рядом с
корзиной** (подпись была бы единственным словом среди значков), а сама
форма открывается **модальным окном** — разворачивать её внутри строки
некуда, строка встала бы враспор. Круг лиц у правки и удаления один и
тот же: автор, владелец, модератор, админ сайта. Удаление СО СТРАНИЦЫ
уводит в список (`deletePost(..., fromPostPage)`) — оставаться на
удалённой теме не на чем.

**Доступ к странице темы — ровно тот же, что у вкладки:**
`communityAccess(...).canSeeInside`, то есть действующие участники и
владелец; постороннему и гостю — `notFound()`. Проверка стоит и в
`generateMetadata`: иначе заголовок закрытой темы уехал бы в `<title>`
страницы-404 (та же история, что у встреч сообщества). Тема из другого
сообщества по этому адресу не открывается: `/communities/A/posts/<тема
из B>` показывала бы содержимое B тому, кто состоит в A.

Поисковикам страница темы не отдаётся никогда (`noIndex`), даже у
публичного сообщества: публична у него витрина, а не разговоры внутри.

### Фотографии

Картинки прикладываются **к комментарию** и **к самой теме** — общим
механизмом отзывов о событиях (АА20): `CommentPhotoPicker` (грузит файл
на `/api/upload` сразу при выборе и оставляет форме адрес скрытым
полем), `CommentPhotos` (показ) и `parseCommentPhotoUrls` — он принимает
ТОЛЬКО наши `/uploads/…`: адрес уходит прямо в `<img src>`, и чужой хост
в нём — это и реферер всех, кто открыл тему, и картинка, которая в любой
момент станет чем угодно. Потолок — `COMMENT_PHOTO_LIMIT` (3).

Кнопка пикера — **иконка со скрепкой без подписи**, слева от
«Отправить», с подсказкой по наведению (`data-tooltip`, как все
подсказки сайта); строки «до 3 фото» рядом с ней больше нет (правка
владельца 2026-09-09). Прикрепить фото — действие второго ряда, и
подпись с пояснением весили больше самой формы, перетягивая внимание с
отправки; сколько фото влезет, человек узнаёт и без надписи — на третьей
кнопка гаснет. Кнопку отправки пикер принимает слотом `trailing`, чтобы
скрепка и «Отправить» стояли одной строкой, — и так же теперь под
событием (`ReviewsAndComments`, правка владельца 2026-09-09): раньше
отправка стояла там своей строкой ниже. Там, где фото прикрепить нельзя,
кнопка остаётся сама по себе — пикера в разметке нет вовсе.

Картинки самой темы лежат на теме — своей таблицей
(`CommunityPostPhoto`). Первый заход держал их на «служебном
комментарии» с пустым текстом, и это оказалось дорого: про строку-призрак
в ОБЩЕЙ таблице комментариев пришлось бы помнить всем, кто их считает
или показывает — счётчику в списке тем, ленте темы, админской
модерации (там она выглядела пустой строкой). Таблица дешевле, чем
исключение в пяти местах.

**Текст комментария обязателен даже с картинками** — но уже по другой
причине: комментарий из одной фотографии в ленте читается обрывком
разговора.

Комментарии — та же модель `Comment`, что у сериалов, новелл и событий,
через связь `postId`. Своей модели нет специально: лайки
(`CommentLike`), ответы одним уровнем (`parentId`, ответ на ответ
цепляется к корню треда) и кнопка лайка уже написаны, а вторая копия
того же кода разъехалась бы с первой. Лайк зовёт общий
`toggleCommentLike` из `reviews/actions.ts` — он работает по `commentId`
и про тип объекта ничего не знает.

### Кто что может

| действие | кто |
| --- | --- |
| читать обсуждения | участники (вкладки у остальных просто нет, а страница темы отдаёт им 404) |
| завести тему, комментировать, отвечать | участники (`CommunityMember.status = ACTIVE`) и владелец |
| закрепить/открепить тему | владелец и модератор |
| удалить тему или комментарий | автор, владелец, модератор, админ сайта |

**Каждое из этих правил проверяется на сервере, в самом экшене**
(`src/app/(public)/communities/postActions.ts`, функция
`requireMember`), а не тем, какие кнопки нарисованы. Страница
сообщества открывается и гостю (см. «Кто что видит»), а форму можно
отправить и мимо интерфейса. Владелец считается участником всегда — не
только по строке `CommunityMember`: хозяин не должен терять доступ к
своему сообществу из-за кривой строки в базе.

Родитель ответа ищется **в пределах темы** (`where: { id, postId }`):
иначе чужой `commentId` из другой темы (или вовсе из-под сериала) пришил
бы ответ не туда.

### Уведомление о новой теме

Все участники, кроме автора, получают `COMMUNITY_POST` — заголовок
складывается на языке получателя из имени автора и названия сообщества
(`notificationText.ts`), отрывок темы уезжает в `body`.

Получатели читаются **одним** `findMany` вместе с языком и настройками и
отдаются в `notifyUser` готовым объектом (`user`): иначе на каждого
участника ушёл бы свой `findUnique` — N+1 в сообществе на сотню человек.

**В Telegram это уведомление не уходит:** его нет в `TELEGRAM_KINDS`
(`src/lib/notifications.ts`) — у живого сообщества тем много, и бот
превратился бы в спамера. Тема остаётся в колокольчике на сайте. Ответ
на комментарий — обычный `COMMENT_REPLY`, он в Telegram ходит: ответ
адресован лично, а не всем сразу.

Ссылка уведомления ведёт на **страницу темы**
(`/communities/<slug>/posts/<id>`), а не на вкладку с якорем: из
колокольчика человек приходит читать конкретный разговор, а не листать
общую ленту. Ответ на комментарий (`COMMENT_REPLY`) ведёт туда же.

Старые уведомления с якорем `?tab=discussions#post-<id>` продолжают
работать: строка списка сохранила `id="post-<id>"`, и ссылка доводит
хотя бы до нужной строки.

### Жалобы

У чужой темы есть «Пожаловаться» — `Report` с `targetType`
`"communityPost"` (список разрешённых типов и их русские подписи —
`REPORT_TARGET_LABELS` в `src/lib/reports.ts`; отдельным модулем, потому
что файл с `"use server"` умеет экспортировать только асинхронные
функции). В `/admin/moderation` жалоба показывается как отзывы и
комментарии: отрывок темы, автор со ссылкой на его карточку и ссылка на
**страницу самой темы** — `/communities/<slug>/posts/<id>`; название
сообщества стоит рядом обычным текстом, чтобы было видно, где это
происходит. Раньше ссылка вела на `?tab=discussions#post-<id>` — адрес
тех времён, когда своей страницы у темы не было, и после её появления
якорь приводил в список, где разговор ещё надо было открывать руками.

Жалоба не отменяет модерации на месте: чужое здесь убирают владелец и
модератор сообщества, а админ сайта нужен на случай, когда проблема —
само сообщество.

### Файлы обсуждений

- **`src/app/(public)/communities/postActions.ts`** — все экшены:
  создание, удаление, закреп, комментарии, удаление комментария. Там же
  строятся ссылки (`postPath`) — и для уведомлений, и для `revalidate`.
- **`src/app/(public)/communities/[id]/DiscussionsTab.tsx`** — список
  тем; сам читает роль зрителя (страница отдаёт вкладке только
  «участник или нет», а закреп зависит ещё и от роли).
- **`PostCard.tsx`** — строка списка (закреп, правка, удаление),
  **`PostForm.tsx`** — форма новой темы: кнопка «Новая тема» и `Modal`,
  как у «Создать сообщество» и у встречи; с прикреплением фото.
- **`PostEditForm.tsx`** — правка темы в модальном окне, из ДВУХ мест:
  подписанной кнопкой на странице темы и иконкой-карандашом в строке
  списка (`compact`). Картинок в ней нет: их выбирают при создании.
- **`[id]/posts/[postId]/page.tsx`** — страница темы: гейт доступа,
  сама тема с картинками и лента комментариев.
- **`PostComment.tsx`** — одна реплика (аватар, текст, картинки, лайк,
  ответ, жалоба, удаление). Форма ответа стоит СВОИМ блоком под строкой
  действий, а не внутри неё: в flex-строке с лайком и жалобой свёртка
  считалась по содержимому, и поле ответа выходило втрое уже страницы
  (правка владельца 2026-09-09).
- **`PostCommentForm.tsx`** — форма комментария и ответа (клиентская:
  после отправки надо очистить поле и пересоздать выбор картинок, иначе
  следующий комментарий уехал бы с теми же фотографиями). Она же
  закрывает за собой свёртку ответа (`closest("details")`): раньше
  формы оставались открытыми и на длинной ветке копились одна под
  другой. У комментария первого уровня свёртки нет, и `closest` там
  просто ничего не находит.
  темы, одно на экшен и на страницу. Отдельным модулем потому, что файл
  с `"use server"` умеет экспортировать только асинхронные функции.
- **`src/components/CommentPhotoPicker.tsx`**,
  **`src/components/CommentPhotos.tsx`**, **`src/lib/commentPhotos.ts`**
  — общие с отзывами о событиях (АА20).

## Роли, бан и приглашения

Всё про людей сообщества живёт в
**`src/app/(public)/communities/memberActions.ts`** — отдельно от
`actions.ts`, где само сообщество (настройки, ссылки, удаление). Права
проверяются в экшенах, а не скрытием кнопок: страницу можно открыть, а
серверный экшен вызвать напрямую.

### Роли

`OWNER` — создатель, `MODERATOR` — помощник, `MEMBER` — все остальные.

Модератора назначает и снимает **только владелец**
(`setCommunityMemberRole`). Иначе двое модераторов раздавали бы права
друг другу, а владелец узнавал бы об этом последним.

| | владелец | модератор |
| --- | --- | --- |
| решать по заявкам | да | да |
| звать людей, отзывать приглашения | да | да |
| убрать участника | да | да |
| убрать **модератора** | да | нет |
| назначать модераторов | да | нет |
| название и описание | да | да |
| видимость и правила вступления | да | нет |
| удалить сообщество | да | нет |

Последние три строки — в `updateCommunity`/`deleteCommunity`: видимость и
правила вступления это решения про само сообщество, а не про его
наполнение.

### Бан: строка остаётся

«Убрать из сообщества» (`banCommunityMember`) **не удаляет строку**
`CommunityMember`, а переводит её в `status = BANNED`. Удаление было бы
просто просьбой выйти: в открытое сообщество человек вернулся бы тем же
нажатием, в сообщество «по одобрению» — подал бы заявку заново, и всё
началось бы сначала. Оставшаяся строка и **есть** запрет: её видят
`communityAccess` (кнопки «Вступить» нет) и `joinCommunity` (внятная
ошибка вместо молчаливого бездействия).

Забаненный **не считается участником нигде**: и список на вкладке, и
счётчики на витрине и на странице фильтруют по `status: "ACTIVE"`.
Роль при бане сбрасывается в `MEMBER` — разбанят, вернётся обычным
человеком, а не сразу модератором. Висящее приглашение при бане
удаляется: принять его значило бы обойти запрет.

Владельца забанить нельзя ни модератору, ни самому себе — сообщество
осталось бы без хозяина, для этого есть «удалить».

**Разбан** (`unbanCommunityMember`) удаляет строку целиком, а не
переводит её в `ACTIVE`: снять запрет — это разрешить человеку
вернуться, а не вернуть его насильно. Решать, хочет ли он обратно, ему
самому.

Список убранных виден в окне «Смотреть всех» отдельным разделом и только
тем, кто может запрет снять, — с объяснением, почему эти строки вообще
там (иначе они выглядят недосмотром).

### Приглашения

`CommunityInvite` — единственный вход в **закрытое** сообщество: заявок
с улицы оно не принимает.

Владелец или модератор ищет человека в модалке «Пригласить»
(`InviteMemberButton`): при открытии сразу показываются **друзья**
приглашающего, а по запросу идёт **поиск по имени и нику** среди всех —
зовут по-разному, своих из друзей, знакомого по переписке — по нику.
Кандидатов собирает сервер (`searchInviteCandidates`), он же вычёркивает
тех, кто уже в сообществе (в любом статусе) или уже позван. Почта в этом
поиске не участвует даже точным совпадением, в отличие от страницы
друзей: приглашение доступно любому модератору, и превращать его в
способ проверить чужой адрес не хочется.

Приглашение **не зачисляет**. Человеку уходит уведомление
`COMMUNITY_INVITE` со ссылкой на сообщество, а на самой странице он
видит баннер «вас зовут» с «Принять»/«Отклонить» (`InviteBanner`, левая
колонка: внутрь закрытого сообщества его ещё не пускают, вкладок у него
нет). Молча добавлять людей нельзя — за дверью чужие адреса встреч и
закрытый чат.

Принял — становится `ACTIVE` (минуя и «по одобрению», и закрытость: его
уже позвал тот, кто вправе звать), приглашение удаляется. Отказался —
приглашение просто исчезает, позвать могут ещё раз; приглашавшему об
отказе не сообщаем, это личное дело. Забанили, пока приглашение висело —
бан сильнее: приглашение снимается, участником человек не становится.

### Уведомления в Telegram

Тумблер `User.tgNotifyCommunities` (настройки → Telegram) управляет
тремя поводами: `COMMUNITY_INVITE`, `COMMUNITY_JOIN_REQUEST`,
`COMMUNITY_JOIN_ANSWER`. Все три редки и адресованы лично, поэтому ручка
на них одна.

`COMMUNITY_POST` (новая тема в обсуждениях) к тумблеру **не привязан**
намеренно (решение владельца): тем в живом сообществе много, и бот на
каждой из них стал бы спамером. Темы остаются в колокольчике на сайте.

## Встречи сообщества

Встреча — это `Event` с проставленным `communityId`. Модель одна с
афишей намеренно: встреча сразу получает карточку события, «я иду»,
комментарии, карту и выгрузку в календарь — писать всё это второй раз
ради «посмотрим серию у Кати» никто бы не стал.

Цена решения — **фильтр каталожных событий** (`src/lib/catalogEvents.ts`).
Каталожное событие — то, у которого нет сообщества; каждое место, где
события берутся скопом, идёт через `catalogEventsWhere()` /
`catalogOccurrencesWhere()`. Одна забытая выборка — и домашний адрес
стоит в общей афише рядом с концертом в Impact Arena. Полный список мест
— в `docs/features/events.md`, раздел «Каталожные выборки».

### Поля

- `communityId` — чьё это сообщество (у афишного события `null`);
- `createdById` — кто позвал (у афишного `null`, их заводит админка);
- `posterUrl` — картинка встречи, необязательная (см. «Картинка встречи»
  ниже). Поле общее с афишным событием, своего заводить не стали.

Слаг встрече не генерируется (`slug: null` при создании): каталожный
слаг — это название с нумерацией `-2`/`-3` в общем пространстве имён,
домашняя встреча заняла бы там место, а номер выдавал бы, сколько ещё
событий называются так же. Ссылка на встречу — `/event/<id>`.

Дата у встречи одна (`EventOccurrence`), и правка её переносит, а не
заводит вторую: иначе перенос оставлял бы призрак старой даты с чужими
отметками «иду».

### Права

Проверяются НА СЕРВЕРЕ в экшенах, интерфейс только повторяет их:

| | завести | править и удалить |
| --- | --- | --- |
| участник | да | свою |
| создатель, модератор | да | любую |
| заявка в ожидании, посторонний | нет | нет |

Правило доступа живёт в `src/lib/meetups.ts` (`communityRights`,
`canSeeMeetup`, `canEditMeetup`) — одним куском, как `communityAccess`
у самого сообщества: вторая копия тех же условий разъедется с первой, и
разъедется молча.

### Кто видит встречу

- **вкладка «Встречи»** — участникам целиком. Посторонний вкладку видит,
  но встречи в ней закрыты: карточка `EventCardLocked` с одной датой, без
  названия, площадки и адреса (правка владельца 2026-09-09, см. «Кто что
  видит» выше);
- **страница `/event/<id>`** — участникам; посторонний получает 404 (и в
  `generateMetadata` тоже — иначе заголовок «Смотрим 5 серию у Кати»
  уехал бы в `<title>` страницы-404). Встреча не индексируется и не
  отдаёт Event-разметку;
- **`/event/<id>/ics`** — участникам. Доступ к встрече решает участие, а
  не подписка: участие в сообществах бесплатное. По той же причине
  «я иду» на встрече работает без подписки, а пейволл на её странице не
  показывается.

Исключений из «только участникам» нет: флаг «показывать всем» отменён
правкой владельца 2026-09-08 — за встречей всегда стоит чей-то домашний
адрес.

### Как выглядит встреча

**Карточка встречи — ТА ЖЕ, что в афише** (просьба владельца
2026-09-08: «на вкладке встречи выводить их так же, как афиша»).
Вкладка кормит общий `EventCard` — блок даты, постер, название,
время и место, «я иду», сердечко. Своей плашки у встречи больше нет:
именно она и создавала жалобу «фото не загружается» — загруженную
картинку вкладка нигде не показывала.

Данные карточке нужны в виде `EventWithPerformers` — плоской строки
«событие + одна дата», какую в афише собирает `flattenOccurrence`
(`src/lib/eventOccurrences.ts`). Вкладка складывает её сама:

- `performers` пустой — на домашней встрече артистов не бывает;
- `community` пустое намеренно: чип с названием сообщества на странице
  этого же сообщества повторял бы шапку (в афише он нужен — там встречи
  идут вперемешку и без него не видно, чья это встреча).

Под карточкой — тихая строка со своим: кто позвал, сколько идут и
кнопка правки. Внутрь общей карточки это не полезло: у афишного
события автора нет, и ради трёх слов карточка разошлась бы с афишной.
Цена — сериал и подробности со строки списка ушли, их видно на
странице встречи. Так же устроена афиша, а строка списка должна быть
строкой, а не пересказом.

**Страница встречи** (`/event/<id>`) начинается постером — картинкой
или заглушкой с первой буквой (`MeetupPoster`). Каталожному событию
заглушка не рисуется: там пустой постер — дырка в данных, которую
заполнит админка, а у встречи это осознанный выбор автора.

**Чьё это сообщество — строкой в блоке информации**, первой, над
площадкой и организатором: «Сообщество · <название-ссылка> · видят
только участники». Отдельного абзаца над заголовком больше нет (правка
владельца 2026-09-08 «некрасиво выводится»): факты о событии живут в
одном блоке, и сообщество — такой же факт, как место. Ссылка ведёт
обратно в сообщество — ради неё строка и нужна. Оговорка о закрытости
идёт тихой добавкой (`small text-secondary`), а не наравне с площадкой:
это пояснение к странице, а не свойство встречи. Целой фразой она
остаётся только в описании страницы для метадаты
(`eventNoticeMembers`), где обрывок повис бы без подлежащего.

**Блока «Мои билеты» на встрече нет** (просьба владельца 2026-09-08):
это посиделки у кого-то дома, а не концерт — билета туда не
существует, и прикреплять к отметке «иду» нечего. Пустая секция звала
бы человека искать то, чего нет.

### Карточка в списке встреч

`MeetupCard.tsx` — обычная карточка события (`EventCard`), а всё
«сообщественное» приходит в неё слотами (правка владельца 2026-09-09:
«Изменить выводим на карточке, слева от сердечка; „Позвал(а) X, идут: N“
— тоже на самой карточке»). Раньше и то и другое стояло ТИХОЙ СТРОКОЙ
ПОД карточкой и читалось как чужой текст рядом с ней.

- `actions` — узел в углу карточки, слева от сердечка. Правка встречи
  приходит туда иконкой-карандашом (`MeetupForm` с `compact`) — тем же
  приёмом, что правка темы обсуждения: в ряду значков подпись была бы
  единственным словом. Удаление в угол не переехало: корзину рядом с
  сердечком слишком легко нажать мимо, а встречу это уносит насовсем —
  «Удалить» осталось внутри окна правки.
- `meta` — тихая строка в теле карточки: «Позвал(а) X» и «идут: N».

Оба слота живут в общем `EventCard`, но флага «это встреча» у карточки
нет: она одна на всю афишу, и знать про сообщества ей незачем.

### Картинка встречи

В форме поле зовётся **«Картинка встречи»** и стоит **сразу под
названием** (правка владельца 2026-09-08). Прежнее название «Афиша» его
и путало: у слова есть второе значение — список событий, — и автор
встречи не понимал, чего от него хотят; внизу формы поле к тому же
просто не замечали. Пояснения под полем нет (правка владельца
2026-09-09) — подпись всё говорит сама.

Само поле — **общая дропзона** `components/FileDropzone.tsx`, та же, что
во всех формах админки (правка владельца 2026-09-09: «вместо кнопки
добавить картинку выводим поле с загрузкой, как у нас везде»). Своя
кнопка со своей загрузкой была вторым видом у одной и той же вещи.
Вариант `wide` — полоса во всю ширину и низкая: окно формы узкое, и
компактный бокс 3:4 админки съедал в нём полэкрана.

Картинка необязательная. Её нет — и карточка, и страница рисуют **первую
букву названия** тем же `.event-card-poster-fallback`, что и афишное
событие без постера: третьей реализации первой буквы в проекте нет.

Файл уезжает на общий `/api/upload` **сразу при выборе**, а не по
«Сохранить», и в форме остаётся только адрес скрытым полем. Сервер
принимает только собственные пути `/uploads/…` без `..`: адрес попадает
прямо в `<img src>`, и чужой хост в нём — это и реферер всех, кто открыл
встречу, и картинка, которая в любой момент станет чем угодно.

Запрета сохранять на время загрузки больше нет: он жил в старой,
самодельной кнопке. Дропзона показывает «Загружаем…» прямо в рамке, и
это ровно то же поведение, что во всех формах админки — второго правила
для одного и того же виджета мы не держим.

Переезд поля наверх гонку **не отменил**, а только сделал реже: пока
автор заполняет дату, место и подробности, загрузка обычно успевает
закончиться — но заполнить их можно и за секунду. Запрет остаётся, и
объяснение у поля тоже: «Сохранить» теперь далеко внизу, и надписи на
кнопке загрузки было бы мало.

После успешного создания форма чистит картинку за собой: она живёт в шапке
вкладки и не размонтируется — следующая встреча уехала бы с картинкой
предыдущей.

### Встречи в афише

На `/events` встречи живут **отдельной вкладкой «Сообщества»**, а не
строками в общей ленте. Причины две, и обе важнее удобства:

1. домашняя встреча и концерт в Impact Arena не равны по смыслу —
   одинаковая карточка в одной ленте обманывала бы читателя, пришедшего
   за афишей;
2. общая лента видна всем, а встречу видят только свои.

Выборку вкладки задаёт `viewerMeetupsWhere(userId)`
(`src/lib/catalogEvents.ts`): только сообщества, где зритель состоит
`ACTIVE`. Гостю она не отдаёт ничего по невозможному условию, а не по
забытому `if`.

Сама страница `/events` — за подпиской целиком (без неё показывается
тизер), поэтому и эта вкладка. Но встречи от подписки не зависят:
своё расписание участник целиком видит на вкладке «Встречи» своего
сообщества, и та бесплатна.

Единственное место, где встречи и афишные события идут вперемешку, —
вкладка «Я иду» (`viewerEventsWhere`): человек отметился на встрече, и
она обязана быть в его списке.

### Что встреча НЕ делает

Ни в календаре, ни в блоке «Вы идёте» на главной, ни в ленте активности,
ни в telegram-напоминаниях, ни в статистике профиля, ни в выгрузке
CSV, ни в ICS-фиде встреч нет — даже своих. Правило одно, без исключений
(«личное можно, каталожное нельзя» пришлось бы решать в каждом месте
заново, и однажды решили бы неправильно). Своё расписание встреч человек
видит на странице сообщества, где рядом и адрес, и кто ещё идёт.

Это правило про **каталожные** подборки — те, что собираются из афиши и
показываются кому угодно. Отдельный блок «В ваших сообществах» на
главной (см. «Связки с остальным сайтом») ему не противоречит: он
показывает встречи не в общем ряду, а своей секцией, и только тем, кто в
этих сообществах состоит.

Заодно это закрывает накрутку: счётчики афиши и «видела вживую»
считаются по афише, а не по встречам, которые человек заводит себе сам.

Единственное исключение — **ачивка** «сходил на встречу» (см. «Ачивки»
ниже): она считает только ЧУЖИЕ встречи, поэтому накрутить её собственной
посиделкой нельзя. В афишные счётчики встреча не попадает и там.

### Файлы

- **`src/lib/catalogEvents.ts`** — `catalogEventsWhere`,
  `catalogOccurrencesWhere`, `viewerMeetupsWhere`, `viewerEventsWhere`.
- **`src/lib/meetups.ts`** — права и потолки полей.
- **`src/app/(public)/communities/eventActions.ts`** — создание, правка,
  удаление встречи, подсказки к полю «сериал». Там же приём `posterUrl`:
  в базу идут только собственные пути `/uploads/…`.
- **`src/app/(public)/communities/[id]/MeetupsTab.tsx`** — вкладка:
  будущие встречи списком, прошедшие — свёрнуто. Она же складывает для
  карточки строку `EventWithPerformers`.
- **`src/app/(public)/communities/[id]/MeetupCard.tsx`** — строка
  списка: общий `EventCard` плюс тихая строка «кто позвал / сколько
  идут / изменить».
- **`src/app/(public)/communities/[id]/MeetupPoster.tsx`** — постер на
  странице встречи: картинка или заглушка первой буквой.
- **`src/app/(public)/communities/[id]/MeetupForm.tsx`** — форма (она же
  на правку и удаление), в том числе загрузка картинки встречи и запрет
  сабмита во время неё.
- **`src/app/(public)/event/[id]/page.tsx`** — страница встречи: гейт
  доступа, строка «Сообщество» в блоке информации, отсутствие билетов и
  разметки для поисковика.
- **`src/components/EventCard.tsx`** — общая карточка афиши и встреч;
  буквенный фолбэк постера живёт в ней.

## Места сообщества

Вкладка «Места» — общие списки: «куда сходить в Минске», «кафе из
сериалов». То, ради чего люди и собираются по городу.

Список сообщества — это обычный **`PlaceList` с проставленным
`communityId`**, своей модели у него нет. Решение то же и по той же
причине, что у встреч с `Event`: так список бесплатно получает страницу
`/lists/[id]` с картой и пинами, поиск локаций, собственные места,
заметки к местам, ручной порядок и «посетила» — всё, что для личных
списков уже написано. Вторая копия этого кода разъехалась бы с первой, и
разъехалась бы молча.

`userId` у такого списка остаётся — строка `PlaceList` без владельца
жить не может. Но **прав он больше не даёт** (см. ниже).

### Кто что может

| действие | кто |
| --- | --- |
| видеть вкладку и списки | участники (вкладок у остальных нет) |
| завести список | создатель и модератор |
| добавлять места, править заметки, порядок, название, видимость, удалять список | создатель и модератор |
| править само МЕСТО (название, фото, точка) | только тот, кто это место завёл |

Право на список сообщества даёт **роль в сообществе, а не строка
`userId`**. Модератор, который список завёл и потом перестал быть
модератором (или вышел из сообщества), теряет над ним власть вместе с
ролью — иначе забрать её было бы некому, а список остался бы за
случайным человеком.

Место при этом принадлежит своему создателю: модератор может убрать
чужое место из списка, но не переименовать его. Локация — общая сущность
каталога, а не содержимое списка.

**Все эти проверки стоят В ЭКШЕНАХ, на сервере** — в
`requireListRights` (`src/app/(public)/lists/actions.ts`), через который
проходят ВСЕ операции над списком: добавление и удаление мест, заметки,
порядок, переименование, видимость, удаление списка. Разметка их только
повторяет: страница списка открывается и постороннему, а серверный экшен
вызывается и мимо интерфейса. Расчёт прав берётся готовым из
`communityRights` (`src/lib/meetups.ts`) — там уже сложено чтение
сообщества с членством и вызов `communityAccess`, и второй копии тех же
условий заводить не стали.

### Подписка

Личный список и собственное место — по подписке (см.
`docs/features/place-lists.md`). **Внутри сообщества подписка не
требуется**: само сообщество уже завёл подписчик, а участие в нём —
и ведение общих списков в том числе — бесплатное.

Без этого исключения фича была бы мертворождённой: «куда сходить в
Минске» состоит из собственных мест почти целиком — минских кафе в
каталоге тайских локаций нет, и модератору без подписки нечего было бы
туда положить.

### Кто видит список

| | участник сообщества | посторонний и гость |
| --- | --- | --- |
| список публичного сообщества, `PUBLIC` | да | **да** |
| список публичного сообщества, остальное | да | нет (404) |
| любой список **закрытого** сообщества | да | **нет (404)** |

Наружу список сообщества выходит ровно в одном случае: **он сам
публичный И сообщество публичное**. У закрытого сообщества наружу не
уходит ничего — даже список, помеченный `PUBLIC`: закрытое прячется
именно затем, чтобы о нём не узнавали со стороны, и утечь через список
мест оно не должно. Это то же правило, по которому закрытые сообщества
не показываются во вкладке «Сообщества» чужого профиля.

Правило записано ОДНИМ файлом — `src/app/(public)/lists/communityLists.ts`
— в двух формах, и это не описка:

- `canSeeCommunityList(list, community, isMember)` — про один уже
  прочитанный список. Через неё ходит гейт страницы
  `src/app/(public)/lists/[id]/page.tsx`, до разметки: непрошедший
  получает `notFound()`, а не страницу без кнопок;
- `visibleCommunityListsWhere(viewerId)` — то же самое `where`-условием,
  для выборок каталога локаций. Там фильтровать в разметке нельзя:
  скрытое стилями всё равно уехало бы в HTML.

Обе формы лежат рядом намеренно — пока они в одном файле, их правят
вместе; разъехавшиеся копии правила приватности молча показывают чужое.
«Участник» в обеих — общее `viewerCommunitiesWhere` (`status = ACTIVE`),
то же, за которым живёт всё остальное содержимое сообществ.

Видимость `FRIENDS` у списка сообщества **не открывает наружу никого**:
дружба — это про человека, а список принадлежит сообществу, и друзья
заводившего к нему отношения не имеют. Поэтому у списка сообщества
состояний видимости два, а не три («только участникам» / «всем»), а
присланный с клиента `FRIENDS` сервер сводит к «только участникам» —
в сторону закрытости, а не открытости.

По умолчанию новый список — «только участникам»: наружу его выносит
ЖЕСТ автора, как у встречи с `communityOnly`, а не пропущенное поле
формы.

Списки сообществ **не показываются в разделе «Мои места»**
(`/lists` фильтрует `communityId: null`): «мой список», который в любой
момент перейдёт другому модератору, — это враньё, и путался бы он с
личными. Его место — на вкладке сообщества.

### Как выглядит вкладка

Сверху заголовок со счётчиком мест (по РАЗНЫМ локациям: одно место
может лежать в двух списках; ноль не показываем), под ним общая карта по
всем спискам сразу, ниже карточки списков — `PlaceListCard`, та же
плашка, что у списка в разделе «Мои места»: ссылкой служит вся карточка,
слева название и описание, справа счёт мест и кто список видит.

Отдельной строки «Открыть список →» под карточкой больше нет (правка
владельца 2026-09-09): она повторяла ссылку, которой уже был заголовок,
и была единственным местом на сайте, где карточку открывали подписью
снизу. По той же причине внутри карточки нет ссылок на сами места —
ссылка в ссылке невалидна; места видны на карте и на странице списка.

Счётчик стоит В ЗАГОЛОВКЕ вкладки, а не в её подписи: подписи вкладок
(«Обсуждения (12)») собирает `page.tsx`, и число мест туда добавляется
одной строкой — `label: withCount(s.tabs.places, await
prisma.location.count({ where: { listItems: { some: { list: {
communityId: community.id } } } } }))`. Считать надо именно локации, а
не строки `PlaceListItem`: иначе место из двух списков посчиталось бы
дважды и разошлось бы с числом в заголовке.

### Места сообществ в каталоге локаций

Каталог `/locations` о сообществах не знал вовсе, хотя «куда сходить в
Минске» — такие же места, как свои (правка владельца 2026-09-09).
Теперь их там два:

- **вкладка «Места сообществ»** в рейке вкладок (`?group=communities`)
  — места из всех доступных зрителю общих списков, теми же карточками
  (`AlphabetDataList`), что и остальной каталог; в подписи карточки —
  чьё это сообщество, над списком — чипы самих списков со ссылками.
  Одно место показывается один раз, даже если лежит в трёх списках.
  Вкладки нет, пока показывать нечего;
- **блок «В списках сообществ»** на странице места — карточки списков,
  в которых оно лежит.

Оба места отбирают списки `visibleCommunityListsWhere(viewerId)` —
**в запросе**. Гостю и постороннему выборка отдаёт только публичный
список публичного сообщества; из закрытого сообщества наружу не уходит
ничего, включая само его название. Личные списки сюда не попадают
вовсе (`communityId: { not: null }`) — у них своя вкладка на человека.

### Файлы

- **`src/app/(public)/communities/placeActions.ts`** —
  `createCommunityPlaceList`: ЕДИНСТВЕННЫЙ свой экшен, потому что право
  здесь считается по сообществу. Всё остальное делают готовые экшены
  списков.
- **`src/app/(public)/lists/actions.ts`** — `requireListRights`:
  раздвоенная проверка прав (владелец строки / роль в сообществе), через
  неё ходят все операции над списком.
- **`src/app/(public)/communities/[id]/PlacesTab.tsx`** — вкладка: общая
  карта по всем спискам сразу, счётчик мест и карточки списков.
- **`PlaceListCard.tsx`** — карточка списка. Общая: её же показывают
  страница места и каталог локаций, чтобы список везде выглядел
  одинаково.
- **`PlaceListCreateButton.tsx`** — форма нового списка (галочка
  «показывать всем» вместо трёх состояний видимости).
- **`src/app/(public)/lists/communityLists.ts`** — правило видимости в
  двух формах (предикат для страницы, `where` для выборок).
- **`src/app/(public)/lists/[id]/page.tsx`** — гейт видимости и плашка
  «список сообщества» со ссылкой.
- **`src/app/(public)/locations/page.tsx`**, **`locations/[id]/page.tsx`**
  — вкладка «Места сообществ» и блок «В списках сообществ».

## Обложка и профиль

### Обложка

`Community.coverUrl` ставит владелец или модератор в окне «Изменить»
(`communities/[id]/CommunityAdmin.tsx`) — тот же круг лиц, что правит
название. Оттуда же её можно убрать. Та же обложка спрашивается и при
создании сообщества (см. «Создание: все поля сразу»).

Пропорции — **1:1** (правка владельца 2026-09-09; до этого была полоса
3:2). Числа живут общей константой `COMMUNITY_COVER_RATIO_W`/`_H`
(`src/lib/communities.ts`) — оттуда их берёт форма создания; в
`CommunityAdmin` пока лежит своя копия, и свести её к общей стоит первой
же правкой: кадрируют обложку два окна, и разъехавшиеся числа означали
бы, что в одном из них рамка врёт о том, что окажется на странице. Квадрат один на все места, где сообщество видно: карточка на
витрине, левая колонка страницы (`.community-cover`), строка в профиле,
предпросмотр в окне правки и сама рамка кадрирования. Картинка
кадрируется в браузере ДО отправки, в общем окне `ImageCropDialog` (то
же, в котором человек ставит себе фото профиля): рамка обязана
показывать то, что окажется на странице, иначе `object-fit: cover`
срежет края уже после загрузки. Ради обложки у окна есть необязательные
`ratioW`/`ratioH` (по умолчанию 3:4 — фото профиля); второй копии окна
ради одного числа заводить не стали. Проп `stageMaxWidth` (расширял
рамку под горизонтальный кадр) вместе с полосой 3:2 и уехал: квадрату
хватает штатных 15rem из `.image-crop-stage`.

**Уже загруженные широкие обложки не переделываются**: файл остаётся
как есть, а квадратный блок срезает его по центру (`object-fit: cover`
у `.community-cover img`) — растянутой картинка не станет нигде.

Дальше всё как у остальных картинок: готовый кадр уходит на общий
`/api/upload` (WebP, уменьшенные копии, лимиты и коды ошибок там же), а
вернувшийся адрес пишет в базу **`communities/coverActions.ts`**.
Отдельный экшен, а не поле формы правки: файл уезжает на сервер до
сабмита, и в общей форме отменённая правка названия молча откатывала бы
и загруженную обложку.

Сервер принимает только собственные пути `/uploads/…` без `..`: адрес
попадает прямо в `<img src>` на публичной странице, и чужой домен в нём
— это и реферер всех, кто открыл сообщество, и картинка, которая в
любой момент станет чем угодно.

Страница сообщества обложку в `CommunityAdmin` не передаёт, поэтому
окно берёт текущий адрес само (`loadCommunityCover`) при открытии —
иначе нечего показать в предпросмотре и не понять, есть ли что убирать.

### Левая колонка страницы сообщества

Страница устроена раскладкой профиля (`.profile-layout`): слева само
сообщество, справа вкладки. Порядок блоков в левой колонке —
решение владельца (2026-09-09), и он не случайный:

1. обложка, название, место, описание, кнопки («Вступить», «Покинуть»,
   «Управление»);
2. **ссылки** сообщества — за ними обычно закрытый чат, то есть дело, с
   которым сюда и приходят;
3. **достижения** — украшение, поэтому ниже дела;
4. **участники** — сетка круглых аватарок той же манеры, что блок
   друзей в профиле (`.profile-friend` + своя сетка
   `.community-member-grid` на пять колонок — ровно тот же размер
   аватарки, что у друзей в колонке той же ширины).

На телефоне (`max-width: 991.98px`) колонка складывается тем же
правилом, что и профиль: слева узкий столбец 7.5rem, справа название с
метой. Место фото занимает обложка — `.profile-side > .community-cover`
переносит её в первый столбец на три ряда. Без этого правила общее
`.profile-side > *` уводило обложку во второй столбец, а первый, под
фото и отведённый, оставался пустой полосой.

Блок участников (`communities/[id]/MembersBlock.tsx`) показывает **не
больше двух рядов** (десять человек): колонка узкая, и полным списком
блок вытеснил бы вниз всё остальное. Дальше — кнопка «Смотреть всех» и
модалка (`components/Modal.tsx`) со списком целиком; отдельной страницы
участников у сообщества нет. В первые два ряда попадают создатель и
модераторы: если видно не всех, полезнее видеть тех, к кому идут с
вопросами.

Отдельной вкладки «Участники» нет (правка владельца 2026-09-09): она
показывала второй раз тех же людей и занимала место в ряду вкладок.
Полный список живёт в том же окне «Смотреть всех» — `MembersList.tsx`
приходит в `MembersBlock` готовым узлом, и вместе с ним туда переехало
управление: роли и бан у строк, «Пригласить», позванные и убранные.
Кнопки собирает СТРАНИЦА и отдаёт узлом — только она знает, кто вправе
их видеть; права всё равно перепроверяются в экшенах, спрятанная кнопка
правом не является.

Кнопка «Смотреть всех» показывается и тогда, когда все аватары влезли в
два ряда: у управляющего за ней стоит не только список.

Видит блок ровно тот, кто видит содержимое (`canSeeInside`): список
людей — персональные данные, и снаружи блока нет вовсе. Условие стоит
на СТРАНИЦЕ, своей проверки прав в компоненте нет — два условия на один
вопрос однажды разойдутся.

### Вкладка «Сообщества» в профиле

`/users/[id]?tab=communities` — где человек состоит
(`users/[id]/CommunitiesTab.tsx`). Строка: обложка, название, описание,
число участников.

Видимость — по правилам профиля (см. Г8 в социальной части) плюс одно
своё, более строгое:

- вкладка целиком подчиняется мастер-выключателю `hideProfileActivity`
  (`showActivity`), как «Отзывы» и «Места»;
- **закрытые сообщества (`PRIVATE`) в чужом профиле не показываются
  вовсе — и друзьям тоже.** Это единственное место, где правило «друзья
  видят всё» не действует, и намеренно: оно про данные ВЛАДЕЛЬЦА
  профиля, а состав закрытого сообщества принадлежит не ему, а
  сообществу. Закрытое сообщество прячется именно затем, чтобы о нём не
  узнавали со стороны, — и утечь через профиль случайного участника оно
  не должно.

Отсекается это в самом запросе (`community: { visibility: "PUBLIC" }`
при `!isSelf`), а не в разметке: в чужой профиль закрытые сообщества не
доезжают даже в пропсы компонента.

Вкладка есть всегда, даже пустая: у зрителя она тогда выглядит ровно
так же, как у человека без сообществ, и по ней нельзя догадаться, что
закрытые всё-таки есть. Заявки (`PENDING`) в список не попадают —
человек ещё не участник.

## Ачивки

Их тут ДВА вида, и путать их нельзя: личные медали человека за то, что
он делает в сообществах (висят в профиле), и медали самого сообщества
за то, что у компании получилось вместе (висят на странице сообщества).

### Личные ачивки про сообщества

Восемь ачивок про сообщества живут в общем механизме (`ACHIEVEMENT_SEED`
в `src/lib/achievements.ts` → таблица `Achievement` прогоном
`scripts/seed-achievements.ts`), показываются там же, где остальные
медали профиля, и выдаются тем же `syncAchievements` при заходе владельца
на свой профиль. Подробности и пороги — в
[gamification.md](gamification.md), раздел «Ачивки сообществ».

Коротко, что награждается: первое вступление, своё сообщество, первая и
пятая темы в обсуждениях, 3 и 10 собранных участников, первая и третья
встречи. Пороги маленькие намеренно — сайт небольшой, недостижимая
ачивка только расстраивает.

Три правила подсчёта, важные именно тут: «вступить» считается по **чужим**
сообществам (`Community.ownerId ≠ я`), собранные участники — **без самого
владельца**, а посещённые встречи — только **чужие** (`createdById ≠ я`):
свою встречу можно завести задним числом и отметиться на ней самому.

### Ачивки самого сообщества

Просьба владельца 2026-09-08: ачивки должны быть не только у человека,
но и у сообщества — «провести одну встречу, 10 участников и т.п.».
Семнадцать штук: проведённые встречи (1/3/10), самая многолюдная
встреча (5), разные люди на встречах (10), участники (3/10/25), темы в
обсуждениях (1/10), живая тема (10 комментариев), разговорчивость
(30 комментариев), собранная поездка, общий список мест и возраст
(месяц / полгода / год). Полный список с порогами и объяснениями — в
[gamification.md](gamification.md), раздел «Ачивки самих сообществ».

Каталог у них общий с личными (`Achievement` + `scope = COMMUNITY`),
выданное лежит в `CommunityAchievement`, метрики считает
`computeCommunityStats` (`src/lib/communityAchievements.ts`) — по
сообществу, а не по человеку.

Что важно знать именно здесь:

- **показываются в левой колонке страницы сообщества**, теми же
  медалями-иконками, что личные в профиле (общий `AchievementBadge`);
  компонент — `communities/[id]/AchievementsBlock.tsx`;
- **видят их те, кто видит содержимое** (`canSeeInside`): достижения
  зарабатывают внутри, и наружу они уходить не должны. Правило берётся
  у страницы, второй копии `communityAccess` тут нет;
- **пересчёт — при открытии страницы** участником, там же, где показ.
  Отдельной фоновой задачи не завели: это один `Promise.all` на
  `force-dynamic` странице, зато новая медаль видна сразу;
- **уведомления о новой медали НЕТ** — у сообщества нет адресата: всем
  участникам это спам, одному владельцу — нечестно, зарабатывали
  вместе;
- `members` тут считается ВМЕСТЕ с владельцем (размер сообщества), в
  отличие от личной ачивки «собрать N участников», где владелец не в
  счёт.

## Связки с остальным сайтом

Сообщество само по себе — ещё не польза: польза начинается там, где оно
встречается с остальным сайтом. Связок три, и все про одно и то же —
«найти своих».

### «В ваших сообществах» — блок на главной

Главная залогиненного — это сводка «что у меня происходит» (поездки,
«вы идёте», «смотрю сейчас», «выходит сегодня»), и до сообществ она
доходила только пунктом меню. Просьба владельца 2026-09-08: «на главной
тоже давай добавим какой-нибудь блок с сообществом».

Блок стоит **под личным расписанием и над лентой «Что нового»**: встречи
и разговоры своих важнее свежего сингла из каталога, но не важнее того,
куда человек сам собрался.

Показывает он две вещи, и обе — из сообществ, где зритель **состоит**:

- **ближайшие встречи** (до 3) — тем же `EventCard`, что и афиша, с
  чипом сообщества. Чип здесь нужен, в отличие от вкладки самого
  сообщества: на главной встречи идут вперемешку, и без него не видно,
  чья это встреча;
- **свежие темы обсуждений** (до 4) — строкой «тема · сообщество ·
  автор · дата · сколько ответов», ссылкой прямо на страницу темы.

Списка самих сообществ в блоке НЕТ намеренно: он уже есть на витрине и
в меню, а на главной был бы оглавлением вместо содержания. Ровно то же
и с закрепом: во вкладке закреплённые темы идут первыми, а тут порядок
по свежести — месячный закреп занимал бы главную вечно.

Строки лежат в `src/lib/i18n/{ru,en}/home.ts`, а не в словаре
сообществ: правятся они вместе с главной.

#### Приватность

Содержимое сообществ закрытое, поэтому блок отбирает его **сам, своими
запросами** — страница не передаёт ему ни одной готовой строки. Правила
общие, своей копии у главной нет: встречи идут через `viewerMeetupsWhere`
(`src/lib/catalogEvents.ts`), темы — через `viewerCommunitiesWhere`
(`src/lib/communities.ts`), и первая функция работает через вторую, чтобы
условие «где я состою» существовало в единственном экземпляре.

| кто | что видит |
| --- | --- |
| гость | блока нет вовсе (главная гостю отдаёт лендинг) |
| вошедший без сообществ | блока нет вовсе — и ни одного запроса от него |
| участник | только встречи и темы СВОИХ сообществ |
| посторонний, заявка (`PENDING`), убранный (`BANNED`) | участником не считается, не видит ничего |
| участник без тем и ближайших встреч | блока нет (не пустая секция, а её отсутствие) |

Фильтр стоит **в запросе, а не в разметке**: закрытое не доезжает даже в
пропсы компонента — скрытое стилями всё равно уехало бы в HTML.

#### Цена для главной

Главная — самая посещаемая страница, поэтому запросов от блока нет
вообще, пока человек не в сообществах. Гейт — один индексированный
`count` по `CommunityMember` в общей пачке `Promise.all` страницы
(участий у человека максимум горстка); ноль — и компонент не
рендерится. У участника это ещё две пачки: темы со встречами разом, а
следом «иду» и «в избранном» по горстке уже найденных id — иначе кнопки
на карточке встречи врали бы о своём состоянии.

Встречи выбираются **со стороны `EventOccurrence`**, а не события:
нужны ближайшие по времени, и сортировка с ограничением должны
считаться в базе. Дата у встречи ровно одна, поэтому дублей строка
occurrence не даёт.

- **`src/app/(public)/HomeCommunities.tsx`** — сам блок: обе выборки,
  сборка `EventWithPerformers` для карточки и строки тем.
- **`src/app/(public)/page.tsx`** — гейт `communityCount` и место блока
  в порядке секций.

### «Из вашего сообщества идут» на странице события

На `/event/[id]` каталожного события, сразу под блоком друзей, — те, с
кем зритель состоит в одном сообществе и кто отметил «иду» на это
событие. Это самый дешёвый и самый точный способ ответить на вопрос, с
которым на страницу и приходят: человек видит не абстрактных
незнакомцев, а людей, которых уже где-то встречал.

Выборка — `getCommunityPeersGoing` в `src/lib/favorites.ts`, рядом с
остальными вопросами к `EventAttendance`. Считается **по событию**, а не
по дате: страница показывает все свои даты разом, и нужен ответ «кто
вообще из своих там будет» (`getFriendsGoingByOccurrence` из
`lib/friends.ts` отвечает на другой вопрос — для карточек конкретного дня
в списках). Отметки per-дата схлопываются: идущий на все три дня стоит в
блоке один раз, как у друзей.

Плашки те же (`EntityMiniCard`), место то же, заголовок соседний —
вопрос у человека один, и два разных вида ответа читались бы как две
разные фичи. Подпись под именем — сообщество, через которое зритель с
человеком и знаком: без неё плашка отвечает «идёт какой-то Дима».

**Приватность — целиком в запросе, а не в разметке:**

| кто | что видит |
| --- | --- |
| гость | блока нет вовсе |
| вошедший без сообществ | блока нет вовсе (пустой список) |
| участник сообщества | только соседей по СВОИМ сообществам |
| заявка (`PENDING`), убранный (`BANNED`) | участником не считается ни с одной стороны |
| закрывший профиль (`hideProfileActivity`) | своё «иду» видит сам, соседям не показывается |
| удалённый аккаунт (`deletedAt`) | не показывается нигде |

Обе стороны связи проверяются на `CommunityMember.status = "ACTIVE"`.
Друзья приходят в выборку списком исключений: они уже показаны блоком
выше, и одно лицо на странице дважды выглядит ошибкой, а не заботой.

Блок **не за подпиской**, в отличие от соседнего блока друзей: участие в
сообществах бесплатно (решение владельца 2026-09-08), и то, ради чего
человек в сообщество вступал, не может прятаться за платой. На странице
встречи сообщества блока нет: там «из вашего сообщества» — это вообще
все, кто отметился, и блок повторял бы счётчик «идут: N» соседней
строкой.

### «Собрать поездку» из сообщества

Кнопка в левой колонке сообщества заводит совместную поездку (`Trip`) и
зовёт в неё выбранных участников. В сообществе договариваются, а едут в
поездке — с планом по дням, бронями, чемоданом и общими делами; заводить
всё это заново руками, перепечатывая имена, — ровно та работа, от
которой сайт и должен избавлять.

Приглашать **всех подряд нельзя**: поездка — вещь личная. Человек
отмечает в форме, кого зовёт, и позванные получают обычное приглашение
(`TripMember` в статусе `PENDING`) — участниками они станут, когда
согласятся. Молча затащить в чужие планы нельзя ровно по той же причине,
по какой нельзя молча зачислить человека в сообщество.

#### Вкладка «Поездки»

Собранная отсюда поездка помечается `Trip.communityId` и возвращается на
вкладку «Поездки» сообщества: иначе её пришлось бы искать в своём
кабинете, а тем, кого позвали, — вспоминать, откуда она взялась. Будущие
и текущие идут сверху (ближайшая первой), прошедшие — свёрткой ниже, как
у встреч. Карточка — общий `TripCard` (`src/app/(public)/trips/`), тот
же билетный `.trip-card`, что в кабинете `/trips`: разметка вынесена в
компонент ровно затем, чтобы две копии не разъехались.

**Сообщество НЕ раздаёт прав на поездку.** `communityId` — это только
«откуда она родом»; кто её видит, по-прежнему решают `Trip.visibility` и
состав участников. Правило вкладки одно:

> показываются ровно те поездки, которые зритель и так вправе открыть на
> `/trips/[id]`, и ни одной сверх того.

То есть своя; та, куда позвали (`TripMember` в любом статусе — `PENDING`
уже открывает страницу с баннером «принять/отклонить»); `PUBLIC`;
`FRIENDS` — друзьям владельца поездки (дружба симметрична, поэтому
вкладка считает по друзьям ЗРИТЕЛЯ и приходит к тому же ответу, что
страница поездки, спрашивающая друзей владельца). Чужая `PRIVATE` не
показывается НИКОМУ, включая участников того же сообщества: сотня человек
в сообществе не должна узнать, что четверо из них летят в Бангкок
20 августа.

Гейт стоит **в самом запросе**, а не в разметке: чужая приватная поездка
не доезжает даже в пропсы компонента — скрытое стилями всё равно уехало
бы в HTML. Это то же правило, по которому в запросе отсекаются закрытые
сообщества. Под заголовком вкладки стоит строка о том, почему список
короче, чем «все поездки сообщества»: без неё короткий список читается
как поломка, а не как чужая приватность.

Кнопка «Собрать поездку» стоит на самой вкладке, рядом со списком (в
левой колонке страницы она осталась с первого этапа связки). Условие
показа прежнее: участник с подпиской — поездки платные целиком.
Списка поездок подписка не требует: он и так показывает только то, что
человеку доступно, а участие в сообществе бесплатное.

- **`src/app/(public)/communities/[id]/TripsTab.tsx`** — вкладка:
  выборка по `communityId` с фильтром видимости (см. выше) и кнопка
  «Собрать поездку».
- **`src/app/(public)/trips/TripCard.tsx`** — карточка поездки, общая
  для кабинета `/trips` и вкладки.
- **`src/app/(public)/trips/actions.ts`** — `createCommunityTrip`:
  близнец `createTrip`, отличается только тем, откуда берётся круг
  приглашаемых, и тем, что проставляет `Trip.communityId`. Слить их в один экшен не вышло бы честно — проверка «а
  вправе ли зритель звать этих людей» у них разная, и именно она держит
  приватность. Присланные id перепроверяются по составу сообщества:
  пройдут только действующие (`ACTIVE`) участники того же сообщества,
  сам зритель отбрасывается.
- **`src/app/(public)/communities/[id]/TripFromCommunityButton.tsx`** —
  форма. Та же, что «Создать поездку» на `/trips` (`Modal` +
  `DatePickerInput` + `EntityMultiSelect` + `VisibilityRadios`), и это
  намеренно: человек уже знает, как она устроена, а вторая копия тех же
  полей начала бы с ней расходиться. Отличается только список, из
  которого выбирают людей: там друзья, здесь участники сообщества.
  Название сообщества подставляется в заголовок поездки заготовкой.

Кнопку видит **участник с подпиской**: поездки платные целиком (см.
[auth.md](auth.md)), и кнопка, которая всегда отвечает «это по
подписке», хуже её отсутствия. Права всё равно перепроверяются в
экшене — спрятанная кнопка правом не является.

Строки обеих связок — подобъект `together` в
`src/lib/i18n/{ru,en}/communities.ts`, отдельно от витрины: связки
правятся вместе друг с другом.

## Связь с каталогом и место

Сообщества умирают не от плохого функционала, а оттого, что их никто не
находит. Отсюда два — и только два — способа обнаружить сообщество
снаружи. Они разные по смыслу, и путать их нельзя.

### 1. Привязки к каталогу: о ком это сообщество

`CommunityTopic` — строка «сообщество ↔ артист» или «сообщество ↔
сериал» (одно из двух полей, вторая половина пустая). Ровно эти
привязки показываются блоком «Сообщества» на странице артиста и
сериала.

**Привязок не больше `COMMUNITY_TOPIC_LIMIT` (3), артисты и сериалы
вместе.** Ограничение принципиальное, а не техническое:

- привязка отвечает на вопрос «о ком это сообщество», а не «что тут
  любят». Сообщество города любит всех подряд — список из сотни
  привязок никто не станет вести, и он мгновенно устареет;
- на странице артиста такой список превратился бы в свалку из
  сообществ, которым до него нет особого дела, и обесценил бы блок:
  ради одного настоящего фан-клуба его перестали бы читать.

Тем, кого не описать тремя строками каталога, привязки и не нужны — у
них есть место (ниже).

### 2. Место: где живёт сообщество

`Community.country` и `Community.city` — для сообществ вроде «Лакорны
Беларусь». Их ищут ПО МЕСТУ, а не по актёру, и своего артиста у них
нет вовсе. Поэтому у места свой путь:

- на витрине `/communities` — фильтр по стране, а внутри выбранной
  страны — по городу. Значения живут в адресе (`?country=…&city=…`),
  как фильтры каталога: ссылкой на срез можно поделиться;
- варианты фильтра считаются по ВСЕМ публичным сообществам
  (`groupBy`), а не по выданной сотне: иначе страна пропадала бы из
  ряда ровно тогда, когда её сообщества не попали на первую страницу;
- город без страны не принимается (ошибка формы): фильтр устроен
  «страна → город», и одинокий «Минск» не нашёлся бы никогда, а в
  списке городов мира спутался бы с тёзками;
- **на страницах артистов и сериалов место не показывается вообще.**
  Оно там ничего не значит: человек пришёл за артистом.

### Приватность

На страницах артиста и сериала показываются **только публичные**
сообщества. Закрытое сообщество прячут ровно затем, чтобы о нём не
узнавали со стороны, — обнаружиться через каталог для него было бы тем
самым, от чего его закрывали.

Отсекается это **в самом запросе** (`visibility: "PUBLIC"`), а не в
разметке: закрытое сообщество не должно доезжать даже в пропсы
компонента. По той же причине блок один на обе страницы и ходит в базу
сам — фильтр приватности существует в единственном экземпляре, две
копии запроса однажды разъехались бы, и разъехались бы молча.

Фильтр витрины считает города по публичным сообществам, так что и
город закрытого сообщества наружу не выходит.

### Кто это правит

Место спрашивается уже при создании (см. «Создание: все поля сразу»), а
дальше его меняет тот же круг лиц, что название и описание, —
**владелец и модератор**: это про наполнение сообщества, а не про его
устройство (видимость и правила вступления остаются владельцу).
Проверка — в самом экшене: форму можно отправить и мимо интерфейса.

Набор привязок перезаписывается целиком (снесли — записали заново, всё
одной транзакцией): их максимум три, и аккуратный diff стоил бы больше
кода, чем экономит, а пустой промежуток между удалением и вставкой
виден на публичных страницах.

Страницы артиста и сериала после правки не сбрасываются: они
`force-dynamic`, блок собирается на каждый запрос.

### Файлы

- **`src/lib/communities.ts`** — `COMMUNITY_TOPIC_LIMIT`.
- **`src/app/(public)/communities/topicActions.ts`** — загрузка и
  сохранение привязок и места, серверный поиск артистов и сериалов для
  комбобоксов.
- **`src/app/(public)/communities/[id]/CommunityAdmin.tsx`** — блок в
  окне управления: два `EntityMultiSelect` (каталог пропсом не
  приезжает — тысячи записей, поиск на сервере по мере ввода, как в
  форме встречи) и поля страны с городом. Текущий набор окно берёт
  само при открытии — как обложку.
- **`src/components/CommunityTopicBlock.tsx`** — блок «Сообщества» на
  страницах артиста и сериала.
- **`src/app/(public)/communities/page.tsx`** — витрина с фильтром по
  месту.
- Строки — подобъект `topics` в `src/lib/i18n/{ru,en}/communities.ts`.

## Модерация сообществ со стороны админки

Сообщество — чужой контент на домене владелицы сайта, и отвечает за него
она. До этого этапа знать о сообществах ей было нечем: раздела в админке
не было, закрытое сообщество не видно ни в витрине, ни в поиске, ни в
карте сайта, удалить сообщество мог только его создатель, а пожаловаться
можно было лишь на тему внутри. Решение владельца: **до выкладки
сообществ на прод админка знает о них всё**.

### Раздел «Сообщества» (`/admin/communities`)

Список ВСЕХ сообществ, включая закрытые: название (ссылкой в админскую
карточку), владелец (ссылкой в `/admin/users/[id]`), число участников,
число тем и встреч, дата создания и пометка видимости. Сверху — счётчик
«открытых / закрытых» и переключатель Все / Открытые / Закрытые, под ним
`NameSearchBox` (`?q=`) по названию; пагинация обычная, по 30
(`adminListHref`, так что фильтр видимости при листании не теряется).

Участники считаются только со статусом `ACTIVE` — отдельным `groupBy`, а
не через `_count`: у `_count` нет условия, и «12 участников», где трое
забанены, врало бы.

Кнопка «На сайте ↗» ведёт на публичную страницу, но главная ссылка —
название, и ведёт оно в **админскую карточку** (`/admin/communities/[id]`).
Так и задумано: у закрытого сообщества публичная страница админу ничего
не покажет, пока он в него не вступит, а вступать, чтобы разобрать
жалобу, — значит попасть в список людей и получать уведомления.

### Карточка сообщества (`/admin/communities/[id]`)

Взгляд внутрь ЛЮБОГО сообщества, включая закрытое, без вступления:
описание, плитки-счётчики, ссылки, участники с ролями и статусами, темы с
отрывком текста и встречи с ближайшей датой. У ссылки, темы, встречи и
самого сообщества — точечное удаление; адрес ссылки показан **текстом, а
не ссылкой**: за ссылкой сообщества как раз и может быть то, ради чего
сюда пришли по жалобе.

Всё удаление идёт общим `adminDeleteContent` из
`src/app/admin/(protected)/moderation/actions.ts` (тип
`ModContentType`) — «админ сносит чужое» живёт в одном месте, вторая
копия того же `switch` рано или поздно забыла бы, например, про проверку
`communityId` у встречи. Встреча удаляется как `Event` с условием
`communityId: { not: null }`: без него ошибочный id снёс бы каталожное
событие афиши. Удаление с самой карточки завёрнуто в
`adminDeleteCommunityAndReturn` (`communities/actions.ts`) — только ради
редиректа в список: иначе карточка удалённого сообщества превратилась бы
в 404, и это читалось бы как поломка.

### Права админа

`communityAccess` принимает четвёртым аргументом
`{ isSiteAdmin }` (см. `src/lib/communities.ts`). Админу открывается
`canSeeInside` — и только оно: `isMember`, `canManage` и `canJoin` не
меняются, а `canBrowse` гасится (иначе страница нарисовала бы разом обе
версии вкладок — и «для участников», и «заглянуть снаружи»). Наружу
отдаётся флаг `isSiteAdmin`, чтобы страница могла честно сказать «вы
смотрите как администратор», а не притворяться, что он свой.

`deleteCommunity` в `communities/actions.ts` пускает админа сайта:
проверка админа стоит ПЕРВОЙ, до `requireManaged`, — тот уводит
редиректом всякого, кто в сообществе не хозяин. Из своих сообществ, как
и раньше, удаляет только владелец: модератор следит за порядком, а не
распоряжается чужим.

### Жалобы на сообщество, встречу и ссылку

Кроме темы (`communityPost`) жалоба принимается на само сообщество
(`community`), его встречу (`communityMeetup`) и его ссылку
(`communityLink`) — полный список и подписи в `src/lib/reports.ts`.
Раньше сказать «сообщество целиком не то» было нечем: приходилось
жаловаться на случайную тему внутри.

В `/admin/moderation` у этих четырёх типов есть кнопка удаления прямо в
карточке жалобы — с предупреждением под тип («Удалить сообщество целиком?
Вместе с ним исчезнут его темы, встречи и участники»). У отзыва,
комментария и списка своя вкладка с поиском, а у контента сообществ её
нет, и жалоба закрывалась «на словах».

### Уведомление о новом сообществе

`notifyAdminsAboutCommunity` (`src/lib/adminNotify.ts`, вид уведомления
`community`, включён по умолчанию) шлёт админам в Telegram сразу при
создании сообщества: название, кто завёл, видимость и прямая ссылка.
Закрытое сообщество не всплывёт больше нигде, так что это единственный
способ узнать о нём вовремя. Название экранируется под `parse_mode:
"HTML"` — «Лакорны <3» иначе не просто отвалилось бы, Telegram отклонил
бы сообщение целиком, и уведомление молча не пришло бы. Отправка не
ожидается и не бросает: создание сообщества не должно ни тормозить
из-за похода в Telegram, ни падать, если бот недоступен.

### Файлы модерации сообществ

- **`src/app/admin/(protected)/communities/page.tsx`** — список,
  **`[id]/page.tsx`** — карточка, **`VisibilityBadge.tsx`** — пометка
  «открытое/закрытое» (одна на список и карточку),
  **`actions.ts`** — удаление сообщества с редиректом в список.
- **`src/app/admin/(protected)/moderation/actions.ts`** —
  `ModContentType` и `adminDeleteContent` со всеми четырьмя типами.
- **`src/lib/reports.ts`** — что вообще принимается как `targetType`.
- **`src/lib/adminNotify.ts`** — `notifyAdminsAboutCommunity`.

## Что дальше

Этапы, согласованные с владельцем 2026-09-08:

2. **Жизнь.** Обсуждения (темы + комментарии + лайки, всё на готовых
   `Comment`/`CommentLike`), уведомления с тумблером в настройках, роль
   модератора в интерфейсе, бан, жалобы (`Report` расширить на
   сообщества и посты).
3. **События сообщества.** Расширить `Event` полями «чьё сообщество»,
   «кто автор», «видимость» — тогда встречи сразу получают карточку,
   «я иду», отметки друзей, комментарии и карту.

   **Обязательное условие:** сначала завести ОДИН общий фильтр
   каталожных событий и перевести на него все места, где события
   берутся скопом (афиша, главная, календарь, страницы артиста,
   локации, сериала, карта сайта). Иначе «пью пиво и смотрю сериал»
   окажется в общей афише рядом с концертом в Impact Arena.

   Видимость встречи по умолчанию — только участники: за встречей стоит
   чей-то домашний адрес.
4. **Чат** — только если после этапа 2 станет видно, что обсуждений не
   хватает. И тогда лентой с опросом раз в 10–15 секунд (как поллинг
   состояния в импорте с MDL), без новой инфраструктуры.

Привязка сообщества к каталогу и к месту — сделана, см. раздел «Связь
с каталогом и место» выше.
