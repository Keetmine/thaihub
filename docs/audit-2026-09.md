# Аудит MyBLHub — сентябрь 2026

Полный аудит сайта (2026-09-09, пять параллельных проходов: безопасность,
удобство, SEO и данные, производительность, продукт). Решение владельца:
**делаем всё из списка**, кроме отдельной страницы пейринга (пары
достаточно на странице артиста); пробный лимит — делаем.

Статус правок отмечается по ходу; что сделано — вычёркивать не надо,
помечать «✓ сделано (дата)». Этот файл — источник задач до полного
разбора.

## Фон по данным (на момент аудита)

Каталог: 10 187 артистов, 5 244 сериала, 621 локация, 789 альбомов,
1 077 связей сериалов, 60 пейрингов. Пользовательский слой: 12 человек,
386 статусов просмотра, 156 оценок, 0 отзывов, 0 комментариев, 5 дружб,
1 сообщество. Вывод: главная ценность — каталог и то, чтобы разговорить
живых людей, а не новые большие разделы.

## 1. Дыры и баги (чинить первыми)

1. Брони поездки: `saveTripBooking`/`deleteTripBooking` не проверяют
   авторство — участник может переписать/раскрыть/удалить чужую
   приватную бронь (trips/actions.ts:1055, :1143; для дел и личных
   событий `canTouchItem` есть, для броней — нет). ✓ сделано
   (2026-09-09): `guardBookingTouch` в trips/actions.ts — бронь правят
   автор и владелец поездки, чужая приватная отвечает «не найдена»
   даже владельцу; карандаш в UI по тому же правилу.
2. `addComment` (reviews/actions.ts:102) не зовёт `canSeeMeetup`:
   посторонний пишет комментарии под закрытой встречей сообщества.
   `saveReview` для встреч проверяет только «событие прошло».
   ✓ сделано (2026-09-09): оба экшена для kind === "event" зовут общий
   гейт (существование + `canSeeMeetup`), постороннему — ошибка
   значением с тем же «Событие не найдено», что у 404 страницы.
3. Закрытое сообщество отдаёт гостю описание (левая колонка + meta
   description) — обещано «название + „закрытое“»
   (communities/[id]/page.tsx:256, :79). ✓ сделано (2026-09-09):
   постороннему на закрытом — только название + пометка (без описания,
   обложки, числа участников, места и кнопки входа), meta description —
   общая фраза раздела.
4. Закрытый вид обсуждений: карточка темы без заголовка показывает
   гостю первые 90 символов ТЕКСТА (DiscussionsTab select тянет text,
   PostCard: title ?? excerpt) — обещаны «заголовки без содержимого».
   ✓ сделано (2026-09-09): снаружи `text` не выбирается из базы вовсе,
   тема без заголовка подписывается «Тема без названия» (обе локали).
5. `photoUrl` профиля пишется без валидации и уходит в голый img —
   утечка реферера/подменяемая картинка (account/actions.ts:27; то же
   lib/ownLocation.ts:56, lists/actions.ts:310). Обложки сообществ
   валидируются — перенести тот же разбор. ✓ сделано (2026-09-09):
   общий `parseUploadUrl` (src/lib/uploadUrl.ts, «только свой
   /uploads/… без ..») стоит во всех трёх местах, невалидное — ошибка
   значением; копия в parseCommunityCoverUrl подлежит сведению туда же.
6. Регрессия 2026-09-09: при переводе картинок на общую FileDropzone
   потерян запрет сохранять, пока файл едет (MeetupForm,
   CreateCommunityButton; комментарии в коде обещают запрет, которого
   нет). Нужен onUploadingChange у дропзоны. ✓ сделано (2026-09-09):
   у дропзоны появился onUploadingChange, обе формы закрывают сабмит
   кнопкой и проверкой в обработчике.
7. Лимит входа: `assertRateLimit` бросает исключение, текст в проде не
   доезжает — безликое зависание вместо «подождите» (lib/rateLimit.ts:52,
   login/actions.ts:10). Вернуть ошибку значением. ✓ сделано
   (2026-09-09): `isRateLimited` возвращает значением, форма входа
   показывает `?error=rate` человеческим текстом в обеих локалях;
   throw-обёртка осталась для промокодов/фидбэка/сброса пароля.
8. Три копии `requireManaged` в сообществах смотрят роль без
   `status: ACTIVE` (communities/actions.ts:89, coverActions.ts:36,
   whereActions.ts:39) — держится на том, что бан сбрасывает роль.
   ✓ сделано (2026-09-09): одна `requireManagedCommunity`
   (lib/communities.server.ts, правило — `communityAccess().canManage`
   со `status: ACTIVE`); в самом lib/communities.ts ей не место — тот
   файл импортируют клиентские компоненты, prisma уехала бы в бандл.
9. Мелочи: `toggleFavoriteEvent` без `canSeeMeetup` — ✓ сделано
   (2026-09-09): проверка по образцу `toggleGoing`, несуществующий id
   отвечает тем же «Событие не найдено» вместо 500; админ сайта
   получает 404 на странице встречи из очереди жалоб (canSeeMeetup не
   знает isAdmin) — ✓ сделано (2026-09-09): `canSeeMeetup`/
   `communityRights` принимают `{ isSiteAdmin }`, страница встречи его
   передаёт, право только смотреть; `sendFriendRequest`/`saveOnboardingFavorites`/
   `addPerformerToList` дают 500 на несуществующий id — ✓ сделано
   (2026-09-09): заявка и список отвечают ошибкой значением
   («Пользователь/Артист не найден», удалённый аккаунт — тоже «не
   найден»), онбординг молча отбрасывает подделанные id и сохраняет
   остальное; `toggleFriendNotifications` заводит mute на любой id без
   проверки дружбы — ✓ сделано (2026-09-09): только при принятой
   дружбе, иначе ошибка значением; фильтр «Иду» без userId в
   eventList.ts:64 не ставится — ✓ сделано (2026-09-09): гостю вкладка
   отдаёт пусто по невозможному условию, а не полную афишу.

## 2. Вводит в заблуждение

- Карточка встречи для не-участника подписана «По подписке», а гейт —
  членство (EventCardLocked, MeetupsTab:131). Нужен бейдж «для
  участников». ✓ сделано (2026-09-09): у `EventCardLocked` появился
  `membersOnly` — подпись и aria «Для участников сообщества» (обе
  локали), афишный вариант «По подписке» не тронут.
- Нет возврата после входа: гейты редиректят на голый /login, вход
  всегда уводит в /account. Нужен ?next= (и для гостевой кнопки
  «Вступить»). ✓ сделано (2026-09-09): прокси, гейты trips/lists,
  формы входа и регистрации проносят ?next= (строгая валидация —
  lib/loginNext.ts, см. docs/features/auth.md). Telegram/Google next
  не проносят (виджет теряет свои query-параметры) — уводят на
  главную, как раньше. Гостевая кнопка «Вступить» — зона сообществ,
  здесь не трогалась.
- /calendar в sitemap и без noindex, но робот получает 302 на логин.
  ✓ сделано (2026-09-09): убран из sitemap, страница отдаёт noindex.
- Canonical сериалов/артистов повторяет запрошенный адрес, а не слаг
  (dramas/[id]:135, artists/[id]:99; соседние разделы делают правильно).
  Те же rawId в хлебных крошках. ✓ сделано (2026-09-09): везде
  `slug ?? id`, и в canonical, и в BreadcrumbList.
- Ошибка сети в форме создания сообщества показывается как «Укажите
  название» (CreateCommunityButton catch). ✓ сделано (2026-09-09):
  общий ключ widgets.errors.network («не удалось связаться с сервером»)
  в обеих локалях.

## 3. SEO и данные

- 77% сериалов без synopsisRu, 59% без titleRu; 94% артистов без bio,
  36% без фото. (Контент: досинк dorama.land/переводы — прогоны.)
- 340 битых /uploads-ссылок (167 сериалов, 142 артиста, 30 событий) —
  перепроверить на проде.
- В sitemap добавить: /events, /communities, /locations/map, /privacy,
  /terms. /lists/[id] — без generateMetadata вовсе (шарятся чаще всего).
  ✓ сделано (2026-09-09): статика добавлена; у /lists/[id] появился
  generateMetadata (React.cache-выборка, публичный список индексируем,
  непубличный — noindex; /lists убран из robots, чтобы noindex был
  виден роботу).
- Личные локации (createdByUserId) индексируемы — закрыть.
  ✓ сделано (2026-09-09): noindex в мете; страница остаётся открытой —
  на неё ведут публичные списки/поездки (place-lists.md).
- NULL-слаги: 26 артистов, 11 локаций, 10 сериалов (backfill-slugs).
  Кандидат в дубли: «Happy Ending» без года — сверить руками.
- Wiki без JSON-LD Article (остальной JSON-LD образцовый).
  ✓ сделано (2026-09-09): articleJsonLd в lib/seo.tsx c dateModified,
  подключён на wiki/[slug].
- Русские строки мимо словарей в EN-версии: EntitySelect,
  EntityMultiSelect, LocationPicker, плейсхолдер «чч:мм» у TimeInput.
  ✓ сделано (2026-09-09): все строки — в словарях (ui.select, ui.map,
  ui.timePlaceholder), в админке те же русские (proxy ставит ru).
- /privacy нет в PUBLIC_PATHS прокси (работает случайно). ✓ сделано
  (2026-09-09): добавлен явно.
- /day/[date] закрыт в robots, но без noindex в мете. ✓ сделано
  (2026-09-09): noindex в мете, а запрет из robots снят — иначе робот
  не доходил до меты («indexed, though blocked»).

## 4. Производительность и техдолг

- Профиль: ~44 запроса и ~12 последовательных стадий на рендер; статусы,
  отметки и отзывы читаются трижды (страница/статистика/лента); review
  и trip findMany без take; до 2000 строк сериалов в RSC-payload;
  syncAchievements пишет в базу при каждом открытии профиля.
  ✓ сделано (2026-09-09): свой профиль — 27 запросов вместо 43 и ТРИ
  волны вместо пятнадцати ступеней (сессия+владелец → одна большая
  выборка → свод/лента/пересчёт разом). Одна таблица — одно чтение:
  строки уезжают в `computeUserStats`/`getActivityFeed`/
  `syncAchievements`/`getUnlockedAchievements` параметром `preloaded`,
  гейты приватности при этом остаются на странице. Потолки: поездки и
  отзывы по 200 (счётчики вкладок считаются по длине массивов, поэтому
  потолок заведомо выше живых чисел). Пересчёт ачивок — не чаще раза в
  10 минут на человека (`achievementsSyncDue`, отметка в памяти
  процесса); записи идут только при новой медали и одной волной.
  Строки таблицы сериалов собираются поимённо — поля для свода
  (`genres`, `duration`, `mdlScore`) в клиентский компонент не уезжают.
  Замер на живом профиле (216 статусов, 12 событий, 3 поездки):
  0.268 → 0.230 с по минимуму, 0.291 → 0.248 с по медиане (дев-сервер,
  локальная база — в проде выигрыш от снятых ступеней больше).
  ОСТАЛОСЬ: около мегабайта РАЗМЕТКИ — `SubTabs` держит смонтированными
  все панели, и каждая строка сериала рисуется дважды (своя пилюля
  статуса + «Все»). Лечится показом строк один раз с фильтром пилюлей
  внутри `DramasTable` — решение владельца не трогать сортировку и
  набор пилюль.
- communities/[id]: loadCommunity без React.cache — самый тяжёлый запрос
  дважды (metadata + страница). users/[id] и trips/[id] metadata — так
  же (trips/[id] ✓ сделано (2026-09-09): общий `getTrip` в React.cache;
  users/[id] ✓ сделано (2026-09-09): `loadProfileUser` в React.cache, а
  свой профиль вообще не запрос — строка приходит с сессией).
  communities/[id] — ✓ сделано (2026-09-09): `loadCommunity` в
  `cache()`, `Community.findFirst` на странице теперь один вместо двух
  (гость 7 → 6 запросов, участник 26 → 25).
- Не кэшированы: watermark на /events, гостевая витрина /communities
  (всё соседнее — в unstable_cache 15–30 мин). ✓ сделано (2026-09-09):
  подложка афиши — `getEventsWatermarkNames` (/events: гость 5 → 4
  запроса, подписчик 10 → 9); витрина сообществ — `getPublicCommunities`
  + `getPlaceFacets` (гость 2 → 0 запросов, залогинённый 5 → 3;
  остаётся только персональный «мои сообщества»). Оба — TTL 30 мин с
  тегом `catalog`; правки сообществ (создание/правка/удаление, место,
  обложка) сбрасывают тег сами через `invalidateCatalogCache`, потому
  что `revalidatePath` записи `unstable_cache` не трогает.
- artists/[id]: фильмография тянет полные Drama (112 КБ у топ-актёра
  ради ~6 полей) — узкий select. ✓ сделано (2026-09-09): узкий select
  (id/slug/title/titleRu/posterUrl/year/type/status/airedFrom + role);
  у топ-актёра (70 сериалов) строки сериалов из базы 112 КБ → 1,6 КБ по
  `pg_column_size` (165 КБ → 19 КБ в JSON). Разметка не изменилась:
  фильмография, бейдж «Выходит», русские названия и таймлайн «Путь
  артиста» на месте.
- 22 FK без индекса (значимые: Comment.userId, Event.createdById/
  dramaId/locationId, Notification.actorId, PerformerSeen.performerId,
  TripPlace.locationId, TripPlaceList.listId, CommunityPost.authorId).
  ✓ сделано (2026-09-09): миграция 20260910T00 — все 22 индекса, схема
  и база сошлись (`prisma migrate status` чист).
- trips/[id]: ~8 последовательных стадий, половина независима.
  ✓ сделано (2026-09-09): независимое собрано в Promise.all (осталось
  три волны: поездка → события+дела+места → личное к событиям).
- Техдолг: RichTextEditor грузит на /api/upload со своей необработанной
  ошибкой (✓ сделано 2026-09-09: общий разбор lib/uploadErrors, текст
  показывается под тулбаром).
  - Дубль Google-иконки (login/signup) ✓ сделано (2026-09-09):
    `GoogleIcon` в components/icons.tsx — единственная иконка файла со
    своими фирменными цветами вместо currentColor, обе страницы зовут
    её с `className="me-2"`.
  - Форматирование дат мимо lib/dates ✓ сделано (2026-09-09): на
    публичной artists/[id] обе самодельные функции убраны — дата
    рождения через `formatLongDate`, день события в «пути артиста»
    через новый `formatDayLongMonth` (день + полный месяц, UTC, как
    остальной модуль). Остальные места оставлены сознательно: админка
    одноязычно русская и в словари не выносится (PremiumToggle,
    analytics, lastSeenLabel, AuditTrail, imports/schedule/errors), у
    telegramNotifications текст тоже только русский, а
    trips/TripTodos.tsx держит en-GB намеренно — та же строка уходит в
    `defaultValue` у `<input type="time">`, который принимает только
    «HH:mm».
  - Мёртвые i18n-ключи ✓ сделано (2026-09-09): вырезано 35 ключей парами
    ru+en (account 8, communities 11, widgets 3, filters 3, catalog 2,
    common 2, reviews 2, events/lists/settings/trips по 1). Список
    аудита проверен заново грепом по src+scripts; `catalog.airedOn` и
    блок `widgets.tour` НЕ трогали — к ним обращаются вычисляемым
    ключом (`t.catalog.airedOn[drama.airedOn]`,
    ``t.widgets.tour[`${key}Title`]``), и грепом по имени они выглядят
    мёртвыми обманчиво.
  - Мёртвый код ✓ сделано (2026-09-09): удалены
    `components/SectionHeading.tsx`, `MEETUP_ADDRESS_MAX`,
    `categoryLabel`, `getGoingEventIds`, `syncDramaFromTmdb` (вместе с
    `DramaSyncOutcome`/`DramaSyncSummary` — они существовали только
    ради неё). Живыми оказались `fandomWiki.ts`, `musicFestival.ts`,
    `tpopFandom.ts` — их зовут импортёры. `mergeAgencies` не удалена, а
    подключена: раздел «Агентства» на /admin/duplicates (см.
    docs/features/duplicates.md).
  - Файлы >1000 строк — НЕ трогали, это отдельная работа. На
    2026-09-09: users/[id] 1559, trips/[id] 1474, artists/[id] 1294,
    dramas/[id] 1273, trips/actions 1244, admin/imports/page 1189.

## 5. Логично добавить

1. Уведомление «у избранного артиста новое событие» (обещано лендингом;
   FavoritePerformer + EventPerformer + notifyUser готовы). Приоритет №1.
   ✓ сделано (2026-09-09): `PERFORMER_EVENT` из админских экшенов
   создания/правки события и импорта (включая одобрение черновиков);
   одно на (человек, событие), дедуп `PerformerEventNotification`;
   колокольчик + Telegram по `tgNotifyEvents`. См.
   docs/features/notifications.md.
2. Попап «поставьте оценку» после перехода в «Просмотрено» (звёзды АА2
   готовы; 208 досмотренных, 0 отзывов). ✓ сделано (2026-09-09):
   `RatePromptPopover` у кнопки/селекта статуса и у счётчика серий;
   сервер отвечает `completedNow`/`hasRating`, закрытие глушит попап
   за сериал до конца сессии вкладки.
3. Поле `DramaWatchStatus.completedAt` — заполнять при переходе в
   COMPLETED. Нужно СЕЙЧАС, иначе декабрьские «Итоги года» пустые.
   ✓ сделано (2026-09-09): ставится при переходе (ручном и
   автопереходе счётчика), повтор не переписывает, уход из COMPLETED
   не стирает, повторное досмотрение обновляет.
4. «Друзья и этот сериал» на странице сериала (DramaWatchStatus ×
   Friendship). ✓ сделано (2026-09-09): блок «Из ваших друзей
   смотрели» — карточки друзей со статусом и оценкой, один запрос
   через обратные связи Friendship (без N+1); друзьям активность
   видна и при hideProfileActivity — то же правило, что на профиле.
   См. docs/features/catalog.md.
5. «Сериал стартовал» для статуса «В планах» (сейчас колокольчик только
   у «Смотрю»). ✓ сделано (2026-09-09): серия №1 шлёт «Стартовал сериал
   из ваших планов» (`DRAMA_STARTED`) плановикам без колокольчика серий
   (с колокольчиком получают обычное «вышла серия 1»); дедуп — общая
   `EpisodeNotification`.
6. ~~Страница пейринга~~ — НЕ делаем (решение владельца 2026-09-09:
   пары достаточно на странице артиста).
7. «Хочу сюда» у локации → системный PlaceList «Хочу посетить»,
   вкладка поездки его подхватит. ✓ сделано (2026-09-09): сердечко
   рядом с «была здесь», системность — соглашение по фиксированному
   названию (src/lib/systemLists.ts, без миграции), создаётся лениво,
   осознанно в обход премиум-гейта (отметка, а не «свой список»);
   ограничения v1 — в docs/features/place-lists.md.

## 6. Дешёвое золото (данные уже в базе)

1. «Смотреть по порядку» — блок из DramaRelation (1 077 связей).
   ✓ сделано (2026-09-09): бывшие «Связанные сериалы» стали этим
   блоком (второго не заводили) — карточки по годам, текущий сериал в
   ряду с акцентной рамкой, тип связи переведён на /ru
   (relationLabel), односторонние связи разворачивают
   sequel↔prequel и parent↔side story. См. docs/features/catalog.md.
2. Вкусовой профиль в статистике: топ-жанры, «вы строже MDL на N».
   Сравнение с MDL убрано 2026-09-17 по правке владельца; жанры остались.
   ✓ сделано (2026-09-09): секция «Вкусовой профиль» сверху вкладки
   «Статистика» — топ-5 жанров по досмотренному (чипы-ссылки в
   /search) и строка сравнения с MyDramaList (порог — 5 пар «своя
   оценка + mdlScore», иначе строки нет); считается из уже
   выбираемых строк computeUserStats, без новых запросов.
3. Рулетка «что посмотреть» по фильтрам /search минус отмеченные.
   ✓ сделано (2026-09-09): кнопка «🎲 Удивите меня» на /dramas у
   поиска → роут /dramas/random (count + skip по случайному смещению,
   302, no-store); залогинённому — без отмеченных любым статусом,
   параметры фильтров /search понимает все (dramaFilterWhere) плюс q;
   кнопка с /dramas проносит q — других фильтров в её адресе нет.
4. «В этот день» на главной (Drama.airedFrom) рядом с др. ✓ сделано
   (2026-09-09): блок годовщин премьер в календарной колонке рядом с
   днями рождения (день+месяц = сегодня, год раньше текущего; свежие
   годовщины первыми, кэш сутки/тег); пусто — блока нет. См.
   docs/features/home.md.
5. Народный топ MyBLHub (порог ≥3 оценок на тайтл). ✓ сделано
   (2026-09-09): страница /dramas/top — среднее только через
   fetchSiteScores, порог ≥3, фолбэк «самое смотримое» с честной
   подводкой, открыта гостю, индексируема (sitemap + canonical),
   ссылка-вкладка в ряду статусов /dramas; см. features/catalog.md.
6. Витрина музыкальных релизов (Album/Song + фильтр «мои избранные»).
   ✓ сделано (2026-09-09): страница /music — свежие релизы той же
   выборкой getMusicNews, что лента главной; переключатель «Мои
   артисты» залогиненному, открыта гостю, со своей метой (в sitemap
   пока не добавлена). См. docs/features/home.md.
7. «Рядом с этим местом» (lat/lng) + вкладка «из моих сериалов» на карте.
   Блок «рядом» — ✓ сделано (2026-09-09): до 5 мест в радиусе ~2 км с
   расстоянием в подписи, прямоугольник в запросе + гаверсинус в JS
   (src/lib/geo.ts). Вкладка «из моих сериалов» — ✓ сделано
   (2026-09-09): `/locations/map?mine=1` оставляет локации сериалов,
   отмеченных зрителем любым статусом (`DramaLocation` ×
   `DramaWatchStatus`, отбор в запросе — как «мои сериалы» в календаре
   и на главной). Гостю вкладок нет вовсе, залогинённому без отметок —
   тоже (пустую вкладку не предлагаем); счётчик в подписи. На
   владельческом аккаунте 466 маркеров → 308. См.
   docs/features/locations.md.

## 7. Социальное и новое

- «Идут с сайта» на событии (все, кто не скрыл активность). ✓ сделано
  (2026-09-09): ряд аватарок на странице события (и каталожного, и
  встречи), виден и гостю; первые 10 и «+N», зритель видит себя всегда,
  показанные выше друзья/соседи не дублируются — см. «Идут с сайта» в
  docs/features/events.md.
- Совместимость вкусов на чужом профиле (пересечение статусов/оценок).
  ✓ сделано (2026-09-09): блок «Совместимость вкусов» в левой
  колонке — «Общего с вами: N сериалов» + «Совпали оценки 9+: X»;
  залогиненному на чужом профиле, при открытой активности и ≥3
  общих; один дополнительный запрос (срез статусов зрителя),
  сторона владельца — из уже выбранных строк страницы.
- Реферальная ссылка /signup?ref= (+ ачивка обоим, приглашённый — друг).
  ✓ сделано (2026-09-09): `User.referredById`, дружба ACCEPTED сразу,
  уведомление пригласившему, достижение «Свой человек», блок с
  копируемой ссылкой в настройках. Битый ref регистрацию не ломает.
  См. features/premium.md.
- Онлайн-встречи сообществ (флаг «онлайн», без места). ✓ сделано
  (2026-09-09): переключатель в форме встречи, venue хранится пустым,
  бейдж «Онлайн» по языку зрителя на карточке/странице/в .ics; офлайн
  без адреса по-прежнему не сохраняется, у каталожных событий признака
  нет.
- Прогресс к достижению сообщества («до „10 участников“ осталось 3»).
  ✓ сделано (2026-09-09) и УБРАНО (решение владельца 2026-09-10):
  достижения сообщества — сюрприз целиком, полоса превращала их в
  задание. Показываются только полученные.
  ✓ сделано (2026-09-09): участникам в AchievementsBlock — одна
  ближайшая неполученная медаль (максимальная доля готовности) с
  подписью и полосой; гостю и не-участникам не показывается, дальние
  медали остаются сюрпризом.
- Мини-блок «У друзей» на главной (3 записи; минимальная версия Г3 —
  согласовано владельцем в рамках «делаем всё»). ✓ сделано
  (2026-09-09): HomeFriendsFeed — три записи общей деривацией
  activityFeed (getFriendsActivity, по списку друзей разом); гостю и
  без друзей блока нет. См. docs/features/home.md.
- Таймлайн «Путь артиста» (сериалы/события/релизы по годам). ✓ сделано
  (2026-09-09): блок на странице артиста, см. features/catalog.md.
- Команды бота /today и /week. ✓ сделано (2026-09-09): личная подборка
  (серии моих сериалов, мои события, дни рождения избранных) на языке
  аккаунта; непривязанному — подсказка привязать Telegram в настройках.
  См. docs/features/telegram-notifications.md, «Команды бота».
- Дневник серий: отметка конкретной серии с датой и заметкой.
  ✓ сделано (2026-09-09), но СКРЫТО (решение владельца 2026-09-10):
  код цел, блоки на странице сериала закомментированы, задача переехала
  в roadmap («Отложено»).
  ✓ сделано (2026-09-09): свёртка «Дневник» под счётчиком серий на
  странице сериала — галочки по сериям (`EpisodeWatch`) с датой и
  личной заметкой, видной только владельцу; отметка подтягивает
  счётчик через `setDramaEpisodesWatched`, обратной связи нет
  (см. «Дневник серий» в features/catalog.md).
- «Угадай сериал по постеру» — мини-игра. ✓ сделано (2026-09-09):
  /game, см. [features/game.md](features/game.md).
- «Маршрут дня» из «Что посетить» — ссылка Google Maps с waypoints.
  ✓ сделано (2026-09-09): «Построить маршрут» над картой вкладки,
  жадный порядок от первой точки, до 9 точек с честной подписью при
  переборе, без API-ключей (src/lib/dayRoute.ts).

## 8. Монетизация

- Недельный персональный дайджест в Telegram (премиум). ✓ сделано
  (2026-09-09): воскресная рассылка «Ваша неделя» задачей планировщика
  `weekly-digest` (`intervalDays: 7` + новый `weekday: 0`, час по
  умолчанию 10) — серии отмеченных сериалов, свои события, старты
  продаж по ним и дни рождения избранных на 7 дней вперёд. Текст
  собирает ОБЩИЙ сборщик подборок бота (`buildDigestMessage`), второго
  не заводили; заодно секция стартов продаж появилась в ответах на
  /today и /week. Кому: Telegram + активная подписка + тумблер
  `tgNotifyDigest` в общем ряду настроек (бесплатному — пометка
  «Входит в подписку»). Пустую подборку не шлём. См.
  features/premium.md и features/telegram-notifications.md.
- Подарочная подписка на готовых промокодах. ✓ сделано (2026-09-09):
  кнопка рядом с покупкой; при включённых Stars оплата создаёт
  одноразовый код (общий генератор с админкой) и присылает открытку в
  Telegram, при выключенных — ведёт в контакт с заготовленным текстом.
- Пробный лимит вместо глухого пейволла: бесплатным по одной поездке и
  одному списку (решение владельца 2026-09-09 — делаем). ✓ сделано
  (2026-09-09): `FREE_TRIP_LIMIT`/`FREE_PLACE_LIST_LIMIT` = 1, гейты и на
  страницах, и в экшенах; списки сообществ и системный «Хочу посетить» в
  счёт не идут; у подписчиков не изменилось ничего.
- Кастомизация профиля подписчикам (обложка, рамка, значок). ✓ сделано
  (2026-09-09): рамка (`.profile-side-photo-premium`) и значок
  (`.premium-badge-icon`) были сделаны раньше, правкой владельца п.6;
  здесь добавлена ОБЛОЖКА — `User.coverUrl`, широкая картинка (16:5)
  над левой колонкой профиля. Ставит только подписчик (общая
  `FileDropzone` в настройках, адрес через общий `parseUploadUrl`), а
  ВИДЯТ её все, включая гостей: иначе профиль выглядел бы сломанным на
  следующий день после окончания подписки. Экшен трогает колонку,
  только если поле пришло И подписка активна, — сохранение профиля
  бывшим подписчиком обложку не стирает. Заодно закрыта та же дыра,
  что в п.1.6: дропзоны настроек теперь закрывают «Сохранить» на время
  загрузки (`SettingsUploadField` + счётчик в `SettingsForm`). См.
  features/social.md.
- Месячная сводка владельцу сообщества. ✓ сделано (2026-09-09, с
  оговоркой): задача `community-digest` (раз в 30 дней) пишет создателю
  сообщества, что у него было за месяц — новые участники (только
  ACTIVE), темы, комментарии, ближайшая встреча; считается по
  существующим моделям, без новых таблиц и без N+1. Пустая сводка не
  уходит. **Оговорка:** сводка идёт только в Telegram (по тумблеру
  «Сообщества»), строки в колокольчике у неё нет — для неё нужен свой
  `NotificationKind` (`COMMUNITY_DIGEST`) — заведён следом (миграция
  20260910T01), сводка приходит и в колокольчик; было: новое значение enum
  в базе, а миграции в этом заходе были запрещены. Когда вид заведут,
  меняется одна строка доставки на `notifyUser`; подсчёт готов.

## 9. Мелочи интерфейса (полный список — правим все)

- «Календарь» в мобильном таб-баре обрезается (360–375px). ✓ сделано
  (2026-09-09): короткая подпись «Даты»/"Dates" (nav.calendarShort).
- Куки-баннер на телефоне перекрывает модалки и таб-бар. ✓ сделано
  (2026-09-09): z-index 1080→1040 — над таб-баром, под шторкой и
  модалками.
- text-capitalize капитализирует месяц («25 Октября») — только первую
  букву (users/[id]:726). ✓ сделано (2026-09-09): класс
  .first-letter-cap с ::first-letter.
- MeetupPoster без фолбэка на битую картинку (в EventCard фолбэк есть).
  ✓ сделано (2026-09-09): клиентский островок с приёмом из EventCard
  (checkPoster + onError), буквенная заглушка та же.
- «Убрать ссылку» в управлении сообществом — текстовая кнопка, в
  создании уже крестик; у полей «Подпись»/«Ссылка» в управлении нет
  aria-label. ✓ сделано (2026-09-09): тот же icon-btn с data-tooltip и
  aria-label у обоих полей.
- /locations — единственный каталог с рядом вкладок без ScrollableTabs.
  ✓ сделано (2026-09-09).
- Словарь: «актёры»→«артисты» в подписях (account.ts:78,80,198,
  catalog.ts:275, widgets.ts:131, auth.ts:125,131); «ачивки»→
  «достижения» в админке; три варианта «ничего не найдено» — свести.
  ✓ сделано (2026-09-09): SEO-описания (catalog.ts:258, events.ts:8)
  оставлены с «актёрами» намеренно — работают на поисковые запросы;
  пустой поиск сведён к common.nothingFound / common.nobodyFound.
- Разные aria у одного карандаша: «Редактировать» (PersonalEventCard)
  vs «Изменить» (MeetupForm). ✓ сделано (2026-09-09): common.edit (ru)
  теперь «Изменить», PersonalEventCard его и берёт; MeetupForm пока на
  t.communities.edit с тем же текстом — перевести на common.edit следом.
- FileDropzone: подсказка «Изображение, до 8 МБ» при accept с PDF;
  превью PDF — битая img; «перетащите» на таче. ✓ сделано (2026-09-09):
  hintPdf по accept, превью PDF — иконка с именем файла, на таче —
  «Выберите файл» через (hover: none).
- Доступность: имена «друзья идут» недостижимы с клавиатуры (нет
  tabIndex) — ✓ сделано (2026-09-09, tabIndex=0); постер-ссылка
  EventCard без имени (tabIndex=-1 как в тизере) — ✓ сделано
  (2026-09-09); тултипы RichTextEditor через title вместо data-tooltip —
  ✓ сделано (2026-09-09, у цветных кружков заодно aria-label).
- В форме встречи EntitySelect и связанные — русские строки в EN.
  ✓ сделано (2026-09-09): вместе с пунктом про словари в разделе 3.

## Данные и прогоны (не код — на прод у владельца)

- `scripts/backfill-slugs.ts` — закрыть NULL-слаги.
- Пересобрать/дотянуть битые картинки (список путей — по SQL из
  аудита), перепроверить на проде.
- Досинк русских синопсисов и названий (dorama.land), био и фото
  артистов — постепенные прогоны.
- Дубль «Happy Ending» (без года) — сверить руками в админке дублей.
