node_modules
.next
.git
.env
.env.local
*.log
npm-debug.log*
.DS_Store

# Снимки базы перед деплоем (`backups/` на сервере) — это десятки
# гигабайт: без исключения docker тащил их в контекст сборки, и деплой
# «на месте» вставал на передаче 26 ГБ (поймано 2026-09-18).
backups
*.sql.gz
*.tar.gz
