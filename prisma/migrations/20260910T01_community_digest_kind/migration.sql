-- Месячная сводка владельцу сообщества — свой вид уведомления, чтобы
-- она приходила и в колокольчик, а не только в Telegram (аудит 2026-09,
-- раздел 8). Отдельный вид, а не чужой: notifyUser складывает фразу из
-- вида, и любой существующий дал бы ложный заголовок.
ALTER TYPE "NotificationKind" ADD VALUE IF NOT EXISTS 'COMMUNITY_DIGEST';
