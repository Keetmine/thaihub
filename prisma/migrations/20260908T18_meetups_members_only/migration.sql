-- Встречу сообщества видят только его участники — всегда (правка
-- владельца 2026-09-08). Флаг «показывать всем» отменён: встреча в
-- общей афише сбивала с толку, а участникам она показывается отдельной
-- вкладкой афиши. Поле прожило один день и до прода не доехало.
ALTER TABLE "Event" DROP COLUMN "communityOnly";
