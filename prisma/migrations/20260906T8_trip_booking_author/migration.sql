-- Автор брони. Без него приватная бронь участницы совместной поездки
-- считалась приватной бронью ВЛАДЕЛЬЦА (автор подставлялся владельцем
-- по умолчанию): своя запись пропадала у автора и показывалась хозяйке.
-- Старые строки остаются с NULL — это и есть «владелец поездки», ровно
-- то поведение, что было до сих пор.
ALTER TABLE "TripBooking" ADD COLUMN "createdById" TEXT;
