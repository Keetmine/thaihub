-- Привязки сообщества к артистам и сериалам отменены (решение владельца
-- 2026-09-09).
--
-- Причина в самом ограничении: «я могу любить 20 актёров и сделать
-- сообщество по всем ним». Лимит в три привязки мешал таким
-- сообществам, а без лимита список на странице популярного актёра
-- превращался бы в свалку из десятков сообществ. Красивого решения не
-- нашлось, поэтому связь убрана целиком.
--
-- Что осталось: МЕСТО сообщества (Community.country/city) и фильтр по
-- нему на витрине — географические сообщества ищут по стране, а не по
-- актёру, и этот путь работает.
--
-- Данных не теряем: до прода фича не доехала, строк в таблице нет.
DROP TABLE IF EXISTS "CommunityTopic";
