-- Глазик «видела вживую» на странице артиста показывает ИТОГОВОЕ
-- состояние и умеет снимать автоматическое (АА7). Строка PerformerSeen
-- становится ручным решением, перекрывающим автоматику: seen=true —
-- «видела» (как было раньше у всех строк, поэтому DEFAULT true), seen=false —
-- «нет, этого из состава события не видела».
ALTER TABLE "PerformerSeen" ADD COLUMN "seen" BOOLEAN NOT NULL DEFAULT true;
