-- Свои блоки ссылок у артиста (правка владельца 2026-09-15): «как
-- бренды, только чтобы можно было задавать и заголовок блоку» —
-- «Питомцы» с инстаграмами кота и собаки, «Кафе» с заведением артиста.
--
-- Колонкой у существующей PerformerLink, а не новой таблицей: это та же
-- пара «название + адрес», отличается только тем, куда её положить на
-- странице. Пусто — прежнее поведение (кнопка в блоке фактов).
ALTER TABLE "PerformerLink" ADD COLUMN "group" TEXT;
