-- Приватные и публичные темы обсуждений (правка владельца 2026-09-09).
--
-- Публичная тема попадает в список, который видят и посторонние: по
-- нему видно, живое ли сообщество, и понятно, ради чего вступать.
-- Открыть тему по-прежнему может только участник — снаружи виден
-- заголовок, а не разговор.
--
-- Умолчание false (публичная): сообщество заводят, чтобы его нашли.
ALTER TABLE "CommunityPost" ADD COLUMN "isPrivate" BOOLEAN NOT NULL DEFAULT false;
