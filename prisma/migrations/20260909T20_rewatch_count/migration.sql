-- Пересмотры: сколько раз человек смотрел сериал СВЕРХ первого раза.
--
-- Одним числом у строки статуса, а не отдельной таблицей заходов
-- (решение владельца 2026-09-09: «пока только счётчиком»): даты каждого
-- просмотра пришлось бы вводить руками, а их никто не помнит. Если
-- история понадобится, счётчик в неё переедет без потерь.
--
-- Всем нулевой дефолт и никакого бэкфилла: мы не знаем, кто что
-- пересматривал, и выдумывать не станем.

ALTER TABLE "DramaWatchStatus" ADD COLUMN "rewatchCount" INTEGER NOT NULL DEFAULT 0;
