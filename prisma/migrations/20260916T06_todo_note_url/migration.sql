-- Описание и ссылка у дела поездки (правка владельца 2026-09-16: «в
-- дела тоже добавить ссылку или описание какое-то»).
--
-- Ровно те же два поля, что у личного события поездки: «записаться в
-- визовый центр» без адреса и ссылки на запись — половина дела.
--
-- Колонки на всей TripTodo (дела, чемодан и покупки — одна модель с
-- полем kind), но в форме показываются только у ДЕЛ: у «взять
-- переходник» описания не бывает.
ALTER TABLE "TripTodo" ADD COLUMN "note" TEXT;
ALTER TABLE "TripTodo" ADD COLUMN "url" TEXT;
